name: CI

on:
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    - run: npm ci
    - run: npm audit --audit-level=high
    - run: npm run typecheck
    - run: npm run lint
    - run: npm run test:ci
    - run: npm run build
    - name: Bundle size report
      run: |
        echo "=== Bundle size report ==="
        ls -lh dist/assets/*.js | awk '{print $5, $9}' | sort -rn | head -15
    - name: Bundle size comparison
      run: |
        # Save current build
        mkdir -p /tmp/current-dist
        cp -r dist/assets/* /tmp/current-dist/
        # Build base branch
        git fetch origin main
        git checkout origin/main -- .
        npm ci
        npm run build
        mkdir -p /tmp/base-dist
        cp -r dist/assets/* /tmp/base-dist/
        # Compare
        git checkout -- .
        npx tsx scripts/bundle-compare.ts /tmp/base-dist /tmp/current-dist
    - name: Bundle size budget
      run: |
        MAX_INDEX=600000   # index-*.js fail-порог (байты)
        MAX_VENDOR=500000  # vendor-*.js warn-порог
        MAX_CSS=150000     # *.css fail-порог (байты)
        fail=0
        for f in dist/assets/index-*.js; do
          sz=$(stat -c%s "$f" 2>/dev/null || stat -f%z "$f")
          echo "index: $f = $sz"
          [ "$sz" -gt "$MAX_INDEX" ] && { echo "::error::$f > $MAX_INDEX"; fail=1; }
        done
        for f in dist/assets/vendor-*.js; do
          sz=$(stat -c%s "$f" 2>/dev/null || stat -f%z "$f")
          [ "$sz" -gt "$MAX_VENDOR" ] && echo "::warning::$f > $MAX_VENDOR ($sz)"
        done
        for f in dist/assets/*.css; do
          sz=$(stat -c%s "$f" 2>/dev/null || stat -f%z "$f")
          echo "css: $f = $sz"
          [ "$sz" -gt "$MAX_CSS" ] && { echo "::error::$f > $MAX_CSS"; fail=1; }
        done
        exit $fail
    - name: Install Playwright
      run: npx playwright install chromium
    - name: E2E Tests
      run: npm run build && npx playwright test --project=chromium
    - name: Functions build & test
      run: cd functions && npm ci && npm run build && npm test
    - name: Set up Java for Firebase Emulator
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    - name: Cache Firebase Emulator
      uses: actions/cache@v4
      with:
        path: ~/.cache/firebase/emulators
        key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('functions/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-firebase-emulators-
    - name: Run rules and emulator tests
      run: cd functions && npm run test:rules && npm run test:emulator
  deploy-functions:
    needs: build
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true && github.base_ref == 'main'
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    - run: npm ci
    - run: cd functions && npm ci && npm run build
    - name: Deploy functions
      run: npx firebase deploy --only functions --token ${{ secrets.FIREBASE_TOKEN }}
    - name: Rollback on failure
      if: failure()
      run: |
        echo "Deploy failed — rolling back to previous functions version"
        npx firebase functions:delete chatWithAI editWithAI summarizeDocument validateCustomPrompt getAILimit getAIUsageStats setUserRole setAIModel resetUserLimit embedDocument summarizeFacet rerankNotes --token ${{ secrets.FIREBASE_TOKEN }} --force || true
        echo "Rollback attempted. Check Firebase console for previous versions."
