{
  "name": "justwriting-functions",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "build": "tsc",
    "test": "vitest run",
    "test:emulator": "npx -y firebase-tools@latest emulators:exec --only firestore \"npx vitest run -c vitest.emulator.config.ts src/__tests__/emulator/\"",
    "test:rules": "npx -y firebase-tools@latest emulators:exec --only firestore \"npx vitest run -c vitest.emulator.config.ts src/__tests__/rules.test.ts\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "isomorphic-dompurify": "^3.14.0",
    "langfuse": "^3.38.20",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@firebase/rules-unit-testing": "^5.0.1",
    "@typescript-eslint/eslint-plugin": "^8.58.0",
    "@typescript-eslint/parser": "^8.58.0",
    "eslint": "^9.39.4",
    "typescript": "^5.3.0",
    "vitest": "^4.1.7"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
