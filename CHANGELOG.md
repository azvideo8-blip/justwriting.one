# Changelog

## Unreleased

## 2026-06-23 (v0.7.32)
- **[RU]** Прикреплённая заметка теперь надёжно доходит до модели: текст доставляется внутри сообщения (как файл), а в чате показывается компактной сворачиваемой карточкой. Большие заметки — через documentContent.
- **[EN]** Attached notes reliably reach the model now: the text is delivered inside the message (like a file) and shown as a compact collapsible card; large notes go via documentContent.
- **[RU]** Авто-подтягивание: «разбери мою последнюю/сегодняшнюю/вчерашнюю заметку» без вложения само находит нужную заметку в архиве по дате. Если не найдено — просит прикрепить, без выдумок.
- **[EN]** Auto-pull: "analyze my last/today's/yesterday's note" without an attachment fetches the matching note from the archive by date. If none — it asks to attach, no fabrication.
- **[RU]** Прикрепление нескольких заметок (2-3) + общий вопрос («что общего между этими заметками»).
- **[EN]** Attach multiple notes (2-3) with a shared question ("what do these notes have in common").
- **[RU]** «Рассуждения» теперь отдельная галочка, независимая от объёма — «кратко/стандартно/объёмно» можно с рассуждением или без. Объёмы дают заметно разную длину ответа. Старые диалоги мигрируют.
- **[EN]** Reasoning is now a separate toggle, independent of length — short/standard/detailed can each be with or without reasoning. Lengths give noticeably different answer sizes. Old dialogues migrate.
- **[RU]** «Ответить иначе» хранит варианты — можно листать ‹ 2/3 › между сгенерированными ответами, ничего не теряется.
- **[EN]** "Regenerate" keeps variants — page through ‹ 2/3 › generated answers without losing any.
- **[RU]** Контекстные кнопки-продолжения под ответом подбираются по содержанию диалога; статичные — как запасной вариант.
- **[EN]** Follow-up suggestion chips are generated from the dialogue context; static ones as fallback.
- **[RU]** Поле ввода чата — авто-расширяющееся (растёт вверх, затем скролл); перетаскиваемый разделитель списка/чата с запоминанием ширины.
- **[EN]** Chat input auto-grows (then scrolls); the list/chat divider is draggable and remembers its width.
- **[RU]** Все попапы — на сплошном фоне (не просвечивают). Убран ряд «как ты?».
- **[EN]** All popups use a solid background (no bleed-through). Removed the "how are you?" mood row.
- **[RU]** Сохранение текста при сбое: ошибка/стоп/дневной лимит больше не теряют набранное — сообщение и вложения возвращаются в поле ввода.
- **[EN]** Input preserved on failure: an error/stop/daily-limit no longer loses what you typed — message and attachments return to the input.
- **[RU]** Шифрование: опция «Запомнить на этом устройстве» — авто-разблокировка при перезагрузке без ввода пароля (по желанию; снимается ручной блокировкой).
- **[EN]** Encryption: "Remember on this device" — auto-unlock on reload without re-entering the password (opt-in; cleared by manual lock).
- **[RU]** Диагностика: кнопки сброса лимита в списке зарегистрированных пользователей; очистка памяти ИИ; «схлопнуть версии».
- **[EN]** Diagnostics: reset-limit buttons in the registered-users list; clear AI memory; collapse versions.
- **[RU]** Профиль: график динамики настроения (пропущенные дни — точками). «В этот день» в архиве со сворачиванием.
- **[EN]** Profile: mood-over-time chart (skipped days as dots). "On this day" in archive, dismissible.
- **[RU]** Исправления: не выдумывает содержание заметки, если её текста нет; синхронизация досылает правки в уже привязанный облачный документ (не залипает на «Unsynced Edits»); рассуждения по-русски; саммари заметок на лёгкой модели + ретрай 5xx (ошибка 500 кнопки AI).
- **[EN]** Fixes: never fabricates note content when the text is absent; sync now pushes edits to an already-linked cloud doc (no more stuck "Unsynced Edits"); reasoning in Russian; note summaries on a light model + 5xx retry (the AI-button 500).
- **[RU]** Service Worker: CACHE_VERSION обновлён до v0.7.32.
- **[EN]** Service Worker: CACHE_VERSION updated to v0.7.32.

## 2026-06-22 (v0.7.31)
- **[RU]** Чат ИИ: устранена галлюцинация на прикреплённой заметке — на ходах с заметкой не подмешиваются темы профиля и память прошлых бесед (модель выдумывала события, которых в заметке нет); строгое заземление строго по тексту заметки.
- **[EN]** AI chat: fixed attached-note hallucination — profile facets and cross-dialogue memory are no longer mixed in on note turns (the model invented events absent from the note); strict grounding to the note text.
- **[RU]** Прикреплённая заметка «липкая» на весь диалог и переживает перезагрузку (диалог привязан к документу) — уточняющие вопросы остаются по тексту заметки.
- **[EN]** Attached note stays sticky for the whole dialogue and survives reload (dialogue linked to its document) — follow-ups stay grounded in the note.
- **[RU]** Чат ИИ: кнопка «Стоп» (сохраняет частичный ответ), удаление отдельных сообщений, «ответить иначе» (regenerate), оценки 👍/👎 → в память предпочтений, чек-ин настроения, подсказки-стартеры и продолжения, кризис-баннер с телефонами помощи; рассуждения принудительно на русском.
- **[EN]** AI chat: Stop button (keeps the partial answer), delete individual messages, "regenerate", 👍/👎 feedback → preference memory, mood check-in, starter/follow-up suggestions, crisis banner with help lines; reasoning forced to Russian.
- **[RU]** Очистка памяти ИИ и «схлопнуть версии» в Диагностике; «разобрать как заметку» для длинной вставки.
- **[EN]** Clear AI memory and "collapse versions" in Diagnostics; "analyze as a note" for long pasted text.
- **[RU]** Письмо: подсказки «о чём писать» в выборе режима; «В этот день» в архиве; мягкий дневной напоминатель на главной; график настроения в профиле.
- **[EN]** Writing: "what to write about" prompts in mode selection; "On this day" in archive; gentle daily nudge on home; mood-over-time chart in profile.
- **[RU]** QA-фиксы: краш поиска при пустом content; нельзя сохранить пустую сессию; синк больше не выкидывает молча офлайн-очередь >24ч; отрицательные «слова сессии» в шапке; подтверждение при удалении диалога/персоны; регистронезависимый дедуп тегов; сброс вибро-вех между сессиями; GDPR-экспорт пропускает заблокированные сессии и пишет реальную версию; экспорт чата без system-сообщений; пустой файл при отсутствии диалога; восстановление архивированного диалога; i18n для ряда строк; README приведён в соответствие.
- **[EN]** QA fixes: search crash on empty content; empty session can no longer be saved; sync no longer silently drops the >24h offline queue; negative "session words" in the header; confirm on dialogue/persona delete; case-insensitive tag dedup; vibration-milestone reset between sessions; GDPR export skips locked sessions and writes the real version; chat export drops system messages; empty file when dialogue missing; unarchive a dialogue; i18n for several strings; README aligned with the app.
- **[RU]** Service Worker: CACHE_VERSION обновлён до v0.7.31.
- **[EN]** Service Worker: CACHE_VERSION updated to v0.7.31.

## 2026-06-21 (v0.7.30)
- **[RU]** Критично: стриминговый `/api/chat` лежал полностью (500 ERR_MODULE_NOT_FOUND из-за extensionless ESM-импорта) с версии 0.7.29 — каждый чат молча уходил в callable-фоллбэк. Починено.
- **[EN]** Critical: streaming `/api/chat` was fully down (500 ERR_MODULE_NOT_FOUND from an extensionless ESM import) since 0.7.29 — every chat silently fell back to the callable. Fixed.
- **[RU]** Режим «🧠 Рассуждения» доведён до рабочего состояния: стримит нативный канал рассуждений DeepSeek в сворачиваемый «Ход мысли», сохраняет его в истории и принудительно держит на русском (раньше пусто/по-китайски). Подход переписан с тегов на нативный `reasoning_content`.
- **[EN]** Reasoning mode «🧠» now actually works: streams DeepSeek's native reasoning channel into a collapsible "Ход мысли", persists it in history, and forces Russian (was empty/Chinese). Reworked from tags to the native `reasoning_content` channel.
- **[RU]** Кнопка «Стоп»: во время ответа «Отправить» превращается в спиннер «Обработка…» + Стоп; прерывание сохраняет уже пришедшую часть ответа.
- **[EN]** Stop button: while answering, Send becomes a spinner "Обработка…" + Stop; interrupting keeps the partial answer already streamed.
- **[RU]** Прикрепление заметки как часть сообщения: вместо авто-отправки появляется чип над полем — можно дописать своё сообщение и отправить вместе. Полный текст заметки теперь уходит модели (раньше — ИИ-саммари).
- **[EN]** Attach-to-compose: attaching a note shows a chip above the input instead of auto-sending — add your message and send together. Full note text now goes to the model (was an AI summary).
- **[RU]** Исправлено: прикреплённая заметка больше не валит следующий запрос (400) — тело заметки схлопывается до маркера в истории API, полный текст идёт через documentContent.
- **[EN]** Fixed: an attached note no longer 400s the next request — its body is collapsed to a marker in the API history, full text travels via documentContent.
- **[RU]** Исправлено: новый чат больше не «пропадает» после перехода на другую вкладку — созданный диалог сразу выбирается и сохраняется.
- **[EN]** Fixed: a new chat no longer "disappears" after navigating away — the created dialogue is selected and persisted immediately.
- **[RU]** THERAPY: терапевтическая глубина персон + кризисный слой безопасности (детект острого риска → мягкое признание боли, ресурсы помощи, без обрыва диалога).
- **[EN]** THERAPY: deeper therapeutic personas + crisis safety layer (acute-risk detection → warm acknowledgment, help resources, no dialogue cutoff).
- **[RU]** UXFIX: переключение объёма ответа реально меняет ответ; живой индикатор «думает…» со счётчиком; кнопка массового анализа всех непроанализированных заметок.
- **[EN]** UXFIX: response-length switch actually changes the answer; live "thinking…" indicator with a timer; bulk "analyze all un-analyzed notes" button.
- **[RU]** Исправлено: сломанная сборка после THERAPY-пуша (дубль импорта + отсутствующая иконка) — прод не компилировался.
- **[EN]** Fixed: broken build after the THERAPY push (duplicate import + missing icon) — production failed to compile.
- **[RU]** Архив: сгруппированный список де-виртуализирован — сортировка по длине/названию больше не дублирует строки.
- **[EN]** Archive: grouped list de-virtualized — sorting by length/title no longer duplicates rows.
- **[RU]** Память чата: каждая единица памяти эмбеддится отдельно (регрессия, где все воспоминания делили один вектор).
- **[EN]** Chat memory: each memory unit is embedded separately (regression where all memories shared one vector).
- **[RU]** Service Worker: CACHE_VERSION обновлён до v0.7.30.
- **[EN]** Service Worker: CACHE_VERSION updated to v0.7.30.

## 2026-06-19 (v0.7.29)
- **[RU]** Стеммер Портера: лексический анализатор «дверей контакта» теперь использует русскую морфологию (думаю/думал/думающая → дум) — ловит больше словоформ.
- **[EN]** Porter stemmer: contact doors analyzer now uses Russian morphology (думаю/думал/думающая → дум) — catches more word forms.
- **[RU]** Динамический лимит ИИ: 10 запросов/день (стандарт), 5 в режиме «🧠 Рассуждения». При переключении на рассуждения — диалог-подтверждение с дисклеймером.
- **[EN]** Dynamic AI limit: 10 requests/day (standard), 5 in reasoning mode. Confirmation dialog with disclaimer when switching to reasoning.
- **[RU]** Архив: исправлен баг GroupedVirtuoso — при сортировке по длине/названию дублировалась первая заметка под каждой датой. Теперь правильный lookup по группе.
- **[EN]** Archive: fixed GroupedVirtuoso bug — sorting by length/title duplicated first note under every date group. Now correct group-relative lookup.
- **[RU]** Попапы: твердые непрозрачные фоны (`--surface-popup`) для всех всплывающих окон — текст больше не просвечивает.
- **[EN]** Popups: solid opaque backgrounds (`--surface-popup`) for all flyout menus — no more text bleed-through.
- **[RU]** Чат-фидбек: экстрактор памяти теперь явно ищет пользовательскую критику стиля/тона → preference-память → раздел «Предпочтения в коммуникации» в портрете.
- **[EN]** Chat feedback: memory extractor now explicitly scans for user style/tone critique → preference memories → «Communication preferences» section in portrait.
- **[RU]** Локальный анализатор стиля письма: средняя длина слова/предложения, частота восклицаний/вопросов — без ИИ, внедряется в промпт портрета.
- **[EN]** Local writing style analyzer: avg word/sentence length, exclamation/question rate — no AI, injected into portrait prompt.
- **[RU]** Анонимная телеметрия: opt-in, rotating ID (30 дней), каждые 14 дней. Тема, бакет заметок, среднее кол-во слов, доля reasoning. Никаких email/имён/текстов.
- **[EN]** Anonymized telemetry: opt-in, rotating ID (30 days), every 14 days. Theme, notes bucket, avg word count, reasoning ratio. No emails/names/texts.
- **[RU]** Оптимизации чата: двери контакта кэшируются на сессию (был пересчёт каждый ход); документы грузятся один раз за sendMessage; память извлекается каждые 3 обмена (было каждый); эмбеддинги памяти батчем; сортировка дверей по дате (новейшие 20).
- **[EN]** Chat optimizations: contact doors cached per session (was recomputed every turn); documents loaded once per sendMessage; memory extracted every 3 turns (was every); memory embeddings batched; doors sorted by date (newest 20).
- **[RU]** Смягчена формулировка контекста: «точно содержится ответ» → «наиболее релевантные заметки; если ответа нет — так и скажи».
- **[EN]** Softened context phrasing: «answer is definitely here» → «most relevant notes; if answer isn't there — say so».
- **[RU]** Психо-персоны: расслаблен гейт на коротких сообщениях — «мне плохо» (2 слова) теперь подтягивает фасеты/память (было <4 слов).
- **[EN]** Psycho-personas: relaxed trivial gate — «мне плохо» (2 words) now triggers facets/memory (was <4 words).
- **[RU]** Дедуп сборки промпта: общий `buildChatSystemPrompt` в `src/shared/ai/` + mirror в `functions/src/shared/` — оба бэкенда используют одну реализацию.
- **[EN]** Prompt builder dedup: shared `buildChatSystemPrompt` in `src/shared/ai/` + mirror in `functions/src/shared/` — both backends use one implementation.
- **[RU]** Service Worker: CACHE_VERSION обновлён до v0.7.29.
- **[EN]** Service Worker: CACHE_VERSION updated to v0.7.29.

## 2026-06-19 (v0.7.28)
- **[RU]** Поиск заметок: consolidated multi-query batching — 1 embedding + 1 rerank на поиск (было N×embed + N×rerank). Entity-aware keyword boosting (имена/кавычки → x2 вес в RRF). Threshold-based rerank bypass (similarity ≥ 0.88 → без cloud rerank). In-memory cache (5 мин, 50 записей, 0 network calls при повторе).
- **[EN]** Note search: consolidated multi-query batching — 1 embedding + 1 rerank per search (was N×embed + N×rerank). Entity-aware keyword boosting (names/quotes → x2 RRF weight). Threshold-based rerank bypass (similarity ≥ 0.88 → no cloud rerank). In-memory cache (5 min, 50 entries, 0 network calls on repeat).
- **[RU]** Оптимизация ИИ-диалога: переиспользование эмбеддинга запроса (1 embed на поиск, было 2-3). Query expansion отключён по умолчанию (флаг VITE_AI_QUERY_EXPANSION). Гейтинг фасет-аугментации на коротких сообщениях + кэш фасетов на сессию. Контекст RAG в system-промпте, не фейковым user-ходом. Порядок заметок: наиболее релевантные — ближе к вопросу («lost in the middle»). Очистка reasoning-утечек в sanitizeAiResponse.
- **[EN]** AI dialogue optimization: query embedding reuse (1 embed per search, was 2-3). Query expansion disabled by default (VITE_AI_QUERY_EXPANSION flag). Facet augmentation gated on short messages + session cache. RAG context in system prompt, not fake user turn. Note order: most relevant near the question ("lost in the middle"). Reasoning leak cleanup in sanitizeAiResponse.
- **[RU]** Рефлексивный диалог: REFLECTION_GUIDE — прогрессивные вопросы, со-интерпретация, никаких общих веллнес-фраз, эмоциональная подстройка, агентность. CBT: Socratic reframe (вопросы, не готовые ответы). Coach: motivational interviewing (complex reflections). Темпоральные наблюдения в фасетах: [активна сейчас], [тема затихла], [новая тема]. Цитирование источников: (источник: «название заметки»).
- **[EN]** Reflective dialogue: REFLECTION_GUIDE — progressive questions, co-interpretation, no empty cheerleading, emotional attunement, user agency. CBT: Socratic reframe (questions, not ready answers). Coach: motivational interviewing (complex reflections). Temporal observations in facets: [active now], [topic faded], [new topic]. Source citations: (source: "note title").
- **[RU]** Память между диалогами (Mem0-style): после диалога ИИ извлекает устойчивые единицы памяти (факты, инсайты, договорённости, предпочтения) через дешёвую модель. Хранятся в IndexedDB с эмбеддингами. В будущих диалогах топ-5 релевантных воспоминаний подмешиваются в контекст. Дедуп по косинусу 0.92. Не блокирует UI, не жжёт чат-лимит.
- **[EN]** Cross-dialogue memory (Mem0-style): after a dialogue, AI extracts durable memory units (facts, insights, commitments, preferences) via cheap model. Stored in IndexedDB with embeddings. Top-5 relevant memories injected into future dialogues. Dedup by cosine 0.92. Non-blocking, doesn't consume chat limit.
- **[RU]** Режим «🧠 Рассуждения»: новый вариант в селекторе объёма ответа. ИИ выводит ход мысли в `<reasoning>` + ответ в `<answer>`. Ход мысли стримится в реальном времени в сворачиваемом блоке «🧠 Ход мысли». Ответ сохраняется без reasoning. Стандартная модель.
- **[EN]** Reasoning mode: new option in response length selector. AI outputs chain-of-thought in `<reasoning>` + answer in `<answer>`. Reasoning streams in real-time in collapsible "🧠 Ход мысли" block. Answer saved without reasoning. Standard model.
- **[RU]** Три двери контакта: локальный лексический анализатор (мысли/чувства/поведение) — без ИИ, без лимитов. Виджет в Диагностике → Профиль AI: распределение, зеркало («твоё внимание чаще идёт через X»), динамика по месяцам. Мягкий сигнал в чат для психо-персон: «чаще опирается на X; Y звучат реже — мягко пригласи заметить Y». Не диагноз.
- **[EN]** Three doors of contact: local lexical analyzer (thinking/feeling/behavior) — no AI, no limits. Widget in Diagnostics → AI Profile: distribution, mirror ("your attention goes through X most"), monthly dynamics. Soft signal in chat for psycho-personas: "relies on X most; Y is rarer — gently invite noticing Y". Not a diagnosis.
- **[RU]** CSP фикс: Google Fonts `onload` inline handler заменён на `rel=preload as=style` + `rel=stylesheet` (CSP-совместимо).
- **[EN]** CSP fix: Google Fonts `onload` inline handler replaced with `rel=preload as=style` + `rel=stylesheet` (CSP-compliant).
- **[RU]** AI кнопки: восстановлен hover-эффект на кнопках объёма/персоны (mass replace /40→/60 убрал разницу). Fix: responseLength при отсутствии activeDialogueId (fallback на dialogue?.id).
- **[EN]** AI buttons: restored hover effect on volume/persona buttons (mass replace /40→/60 removed the difference). Fix: responseLength when no activeDialogueId (fallback to dialogue?.id).
- **[RU]** Service Worker: CACHE_VERSION обновлён с v0.7.21 до v0.7.28.
- **[EN]** Service Worker: CACHE_VERSION updated from v0.7.21 to v0.7.28.

## 2026-06-18 (v0.7.27)
- **[RU]** DPIA: создан документ оценки воздействия на защиту данных (docs/DPIA.md) для ИИ-функций (CBT-персоны, mood tracking, AI-портрет) — 6 разделов: обработка данных, риски, меры безопасности, рекомендации.
- **[EN]** DPIA: created Data Protection Impact Assessment document (docs/DPIA.md) for AI features (CBT personas, mood tracking, AI portrait) — 6 sections: data processing, risks, security measures, recommendations.
- **[RU]** Age gate: при регистрации требуется подтвердить возраст 16+ (чекбокс). Без подтверждения регистрация блокируется.
- **[EN]** Age gate: registration requires confirming age 16+ (checkbox). Registration is blocked without confirmation.
- **[RU]** Аналитика: тумблер в Настройках → Конфиденциальность включает/выключает PostHog. Ссылки на Политику конфиденциальности, Условия использования и «Не продавать мои данные» (CCPA).
- **[EN]** Analytics: toggle in Settings → Privacy enables/disables PostHog. Links to Privacy Policy, Terms of Service, and "Do Not Sell My Personal Information" (CCPA).
- **[RU]** JSON-экспорт: кнопка «Экспортировать в JSON (GDPR)» в Настройках → Аккаунт — структурированный экспорт профиля и заметок в machine-readable формате (Art 20 portability).
- **[EN]** JSON export: "Export as JSON (GDPR)" button in Settings → Account — structured export of profile and notes in machine-readable format (Art 20 portability).
- **[RU]** Безопасность Sentry: UID пользователя хешируется через SHA-256 (16 символов) вместо отправки raw Firebase UID. PII-поля (userId, documentId, email) фильтруются из error context перед отправкой в Sentry.
- **[EN]** Sentry security: user UID is hashed via SHA-256 (16 chars) instead of sending raw Firebase UID. PII fields (userId, documentId, email) are filtered from error context before sending to Sentry.
- **[RU]** Тесты Cloud Functions: 44 новых теста (chatWithAI 14, editWithAI 14, summarizeDocument 16) с моками firebase-admin, generative-ai, DOMPurify, langfuse. 59/59 pass.
- **[EN]** Cloud Functions tests: 44 new tests (chatWithAI 14, editWithAI 14, summarizeDocument 16) with mocks for firebase-admin, generative-ai, DOMPurify, langfuse. 59/59 pass.
- **[RU]** E2E тесты: 15 новых (writing session 6, archive 6, AI 3). 19/19 pass.
- **[EN]** E2E tests: 15 new (writing session 6, archive 6, AI 3). 19/19 pass.
- **[RU]** CI: добавлен шаг rollback при сбое деплоя Cloud Functions (firebase functions:delete).
- **[EN]** CI: added rollback step on Cloud Functions deploy failure (firebase functions:delete).
- **[RU]** Рефакторинг импортов: 11 bridge-файлов в app/, core/services/, shared/components/ — 29 файлов обновлены. Cross-feature violations: 128 → ~28.
- **[EN]** Import refactor: 11 bridge files in app/, core/services/, shared/components/ — 29 files updated. Cross-feature violations: 128 → ~28.
- **[RU]** console.error → reportError: заменены в production source files — ошибки теперь доходят до Sentry.
- **[EN]** console.error → reportError: replaced in production source files — errors now reach Sentry.

## 2026-06-18 (v0.7.26)
- **[RU]** Доступность: 5 модальных окон (шифрование, разблокировка, вход, ИИ-панель, завершение сессии) получили `role="dialog"`, `aria-modal`, focus trap, восстановление фокуса при закрытии и обработку Escape — теперь клавиатура и скринридеры работают корректно внутри диалогов.
- **[EN]** Accessibility: 5 modals (encryption, unlock, login, AI panel, finish session) gained `role="dialog"`, `aria-modal`, focus trap, focus restore on close, and Escape handling — keyboard and screen readers now work correctly inside dialogs.
- **[RU]** Focus trap: при закрытии модального окна фокус возвращается на элемент, который открыл диалог (раньше уходил на `<body>`).
- **[EN]** Focus trap: on modal close, focus returns to the element that opened the dialog (previously went to `<body>`).
- **[RU]** Escape в формах внутри модалок: убрана блокировка Escape при фокусе в input/textarea — теперь модалку можно закрыть Escape даже из поля ввода.
- **[EN]** Escape in form fields inside modals: removed the block on Escape when focused in input/textarea — modals can now be closed with Escape even from an input field.
- **[RU]** Подписи полей: всем input без видимого label добавлен `aria-label` (страница входа, поиск в архиве, поля даты/времени, пароли шифрования).
- **[EN]** Field labels: all inputs without a visible label now have `aria-label` (login page, archive search, date/time fields, encryption passwords).
- **[RU]** Клавиатурная навигация: кликабельные `<div>` в тулбаре, тепловой карте и easter-egg версии теперь доступны с клавиатуры (`tabIndex` + `onKeyDown`).
- **[EN]** Keyboard navigation: clickable `<div>` elements in toolbar, heatmap, and version easter-egg are now keyboard-accessible (`tabIndex` + `onKeyDown`).
- **[RU]** Ошибки и AI-ответы: `role="alert"` на сообщениях об ошибках, `aria-live="polite"` на результатах ИИ и сообщениях чата — скринридеры объявляют динамические изменения.
- **[EN]** Errors and AI responses: `role="alert"` on error messages, `aria-live="polite"` on AI results and chat messages — screen readers announce dynamic changes.
- **[RU]** Раскрывающиеся кнопки и иконки: `aria-expanded` на всех toggle-кнопках, `aria-hidden="true"` на декоративных иконках (архив, чат, навигация).
- **[EN]** Expandable buttons and icons: `aria-expanded` on all toggle buttons, `aria-hidden="true"` on decorative icons (archive, chat, navigation).
- **[RU]** Reduced motion: глобальный CSS-блок `@media (prefers-reduced-motion: reduce)` отключает анимации и переходы. Компоненты с `motion.div` проверяют `useReducedMotion()`.
- **[EN]** Reduced motion: global CSS block `@media (prefers-reduced-motion: reduce)` disables animations and transitions. Components with `motion.div` check `useReducedMotion()`.
- **[RU]** SettingsPanel вкладки: убран `display:contents` + `hidden` — теперь рендерится только активная вкладка (раньше screen readers видели все вкладки одновременно).
- **[EN]** SettingsPanel tabs: removed `display:contents` + `hidden` — now only the active tab is rendered (previously screen readers saw all tabs at once).
- **[RU]** Контраст текста: все `text-text-main/20`–`/50` заменены на `/60` (~5.4:1) — было 1.8:1–4.2:1, не проходило WCAG AA. Placeholder текст — `/40`. Темема amethyst: `text-subtle` opacity 0.70 → 0.90.
- **[EN]** Text contrast: all `text-text-main/20`–`/50` replaced with `/60` (~5.4:1) — was 1.8:1–4.2:1, failed WCAG AA. Placeholder text — `/40`. Amethyst theme: `text-subtle` opacity 0.70 → 0.90.
- **[RU]** SEO: Playwright-пререндеринг — все 7 публичных страниц теперь рендерятся в full HTML через headless Chromium (21K символов контента на главной). Crawlers без JS видят полный контент вместо пустой страницы.
- **[EN]** SEO: Playwright prerendering — all 7 public pages now render to full HTML via headless Chromium (21K chars of content on homepage). Crawlers without JS see full content instead of an empty page.
- **[RU]** SEO: добавлены `og:site_name`, `twitter:site`, `twitter:creator`, `x-default` hreflang. Google Fonts — non-render-blocking (`media=print onload`).
- **[EN]** SEO: added `og:site_name`, `twitter:site`, `twitter:creator`, `x-default` hreflang. Google Fonts — non-render-blocking (`media=print onload`).
- **[RU]** 404 страница: новая `NotFoundPage` с RU/EN поддержкой вместо редиректа на главную.
- **[EN]** 404 page: new `NotFoundPage` with RU/EN support instead of redirect to homepage.
- **[RU]** Ссылки на «О приложении»: добавлены ссылки на Политику конфиденциальности и Условия использования.
- **[EN]** About page links: added links to Privacy Policy and Terms of Service.

## 2026-06-18 (v0.7.25)
- **[RU]** Безопасность шифрования: при неверном пароле vault больше не помечается как «шифрование включено» без ключа в памяти — функция возвращает `false` вместо ложного успеха.
- **[EN]** Encryption security: on wrong password, the vault no longer silently marks encryption as "enabled" without a key in memory — the function returns `false` instead of a false success.
- **[RU]** Авто-блокировка ключа шифрования: через 15 минут неактивности CryptoKey автоматически очищается из памяти; при сворачивании вкладки с истёкшим таймером — тоже. Активность (нажатия клавиш, мышь, тач) сбрасывает таймер.
- **[EN]** CryptoKey auto-lock: after 15 minutes of inactivity, the encryption key is automatically cleared from memory; minimizing the tab with an expired timer also clears it. Activity (keypress, mouse, touch) resets the timer.
- **[RU]** Защита ИИ от prompt injection расширена на сообщения пользователя: паттерны инъекций теперь проверяются не только в кастомных промптах, но и в пользовательских сообщениях в streaming-чате.
- **[EN]** AI prompt injection guard extended to user messages: injection patterns are now checked not only in custom prompts but also in user messages in the streaming chat.
- **[RU]** Единый список паттернов инъекций (15 шаблонов) вынесен в общий модуль `src/shared/ai/injectionPatterns.ts` — Edge API и Cloud Functions больше не рассинхронизированы.
- **[EN]** Unified injection pattern list (15 patterns) moved to shared module `src/shared/ai/injectionPatterns.ts` — Edge API and Cloud Functions are no longer out of sync.
- **[RU]** Аналитика: `trackEvent` и Web Vitals теперь проверяют согласие пользователя (`hasConsent()`) перед отправкой данных в PostHog — раньше обходили этот check.
- **[EN]** Analytics: `trackEvent` and Web Vitals now check user consent (`hasConsent()`) before sending data to PostHog — previously bypassed this check.
- **[RU]** Уязвимости зависимостей: обновлены react-router-dom (RCE CVSS 8.1), vite (fs.deny bypass), hono (CORS+credentials, 9 advisories), form-data (CRLF injection). Все HIGH уязвимости устранены (7 → 0).
- **[EN]** Dependency vulnerabilities: updated react-router-dom (RCE CVSS 8.1), vite (fs.deny bypass), hono (CORS+credentials, 9 advisories), form-data (CRLF injection). All HIGH vulnerabilities resolved (7 → 0).
- **[RU]** CI: `npm audit` теперь проверяет на уровне HIGH без `continue-on-error` — раньше moderate-уровень игнорировался, 7 HIGH уязвимостей молча проходили CI.
- **[EN]** CI: `npm audit` now checks at HIGH level without `continue-on-error` — previously moderate level was ignored, 7 HIGH vulnerabilities silently passed CI.
- **[RU]** Политика конфиденциальности (`/privacy`): новая страница с 10 разделами на RU/EN. Раскрыты все обработчики данных: Firebase, PostHog, Sentry, Gemini, Fireworks AI, Langfuse. Описано шифрование, ИИ-функции, права пользователя, хранение.
- **[EN]** Privacy Policy (`/privacy`): new page with 10 sections in RU/EN. All data processors disclosed: Firebase, PostHog, Sentry, Gemini, Fireworks AI, Langfuse. Covers encryption, AI features, user rights, data retention.
- **[RU]** Условия использования (`/terms`): новая страница с 10 разделами на RU/EN — допустимое использование, ИИ-дисклеймер, ограничение ответственности, возраст 16+.
- **[EN]** Terms of Service (`/terms`): new page with 10 sections in RU/EN — acceptable use, AI disclaimer, limitation of liability, age 16+.
- **[RU]** Privacy Modal: раскрыты Langfuse, PostHog и Sentry; добавлены ссылки на страницы политики конфиденциальности и условий использования.
- **[EN]** Privacy Modal: disclosed Langfuse, PostHog, and Sentry; added links to Privacy Policy and Terms of Service pages.
- **[RU]** SECURITY.md полностью переписан: шифрование (PBKDF2, AES-GCM, auto-lock), безопасность ИИ (инъекции, санитизация, rate limits), App Check, CSP, аналитика, общие паттерны инъекций.
- **[EN]** SECURITY.md fully rewritten: encryption (PBKDF2, AES-GCM, auto-lock), AI security (injection, sanitization, rate limits), App Check, CSP, analytics, shared injection patterns.
- **[RU]** README.md обновлён: tech stack (Motion), разделы features (AI, шифрование, privacy), security, переменные окружения (PostHog, Sentry, SITE_URL).
- **[EN]** README.md updated: tech stack (Motion), features sections (AI, encryption, privacy), security, environment variables (PostHog, Sentry, SITE_URL).

## 2026-06-19 (v0.7.24)
- **[RU]** Переключатель объёма ответа всегда виден: кнопки «Кратко / Стандартно / Объёмно» отображаются даже до начала диалога. При смене объёма в чат добавляется системное сообщение `⚙️ [Смена объёма]: Теперь {персона} ответит вам {кратко/стандартно/объёмно}`. Выбор сохраняется в диалог и применяется к новым сообщениям реактивно.
- **[EN]** Response length toggle always visible: "Short / Standard / Detailed" buttons show even before a dialogue starts. On change, a system message `⚙️ [Volume changed]: {persona} will now respond {briefly/standard/verbosely}` is added to the chat. The selection is saved to the dialogue and applied reactively to new messages.
- **[RU]** Исправлен статус «думает…»: на первом сообщении индикатор загрузки не появлялся из-за `setStreamingMessage('')` вместо `null`. Теперь «{персона} думает…» корректно показывается до начала стриминга.
- **[EN]** Fixed "thinking…" status: on the first message, the loading indicator didn't appear due to `setStreamingMessage('')` instead of `null`. Now "{persona} thinking…" correctly shows before streaming starts.
- **[RU]** Инлайн-переименование диалогов: вместо `window.prompt` заголовок чата переключается в редактируемый `<input>` — Enter сохраняет, Escape отменяет, клик вне поля тоже сохраняет.
- **[EN]** Inline dialogue renaming: instead of `window.prompt`, the chat header switches to an editable `<input>` — Enter saves, Escape cancels, blur also saves.
- **[RU]** Кнопка ИИ в Архиве: повторное нажатие открывает превью-модал с саммари заметки (вместо перехода на страницу чата).
- **[EN]** Archive AI button: a second click opens the note preview modal with the summary (instead of navigating to the chat page).
- **[RU]** Тумблер «Автоматическое саммари заметок» в Настройках → Приложение: отключает фоновую индексацию и построение профиля. Хранится в `localStorage` (`auto_summarize_enabled`).
- **[EN]** "Auto-summarize notes" toggle in Settings → Application: disables background indexing and profile building. Stored in `localStorage` (`auto_summarize_enabled`).
- **[RU]** Настройки аккаунта реорганизованы: «Сменить пароль» и «Шифрование» сгруппированы в сворачиваемую секцию «Безопасность» (ChevronDown/Right). Кнопка «Выйти» перенесена в самый низ. Удалена «Опасная зона» с кнопкой сброса ачивок (она уже есть в Профиле).
- **[EN]** Account settings reorganized: "Change password" and "Encryption" grouped into a collapsible "Security" section (ChevronDown/Right). "Sign out" button moved to the very bottom. Removed "Danger zone" with achievements reset (already in Profile).
- **[RU]** ZIP-экспорт в формате .txt: заметки в архиве теперь сохраняются как plain text (.txt) с удалением markdown-разметки (заголовки, жирный/курсив, ссылки, списки) через `stripMarkdown()`.
- **[EN]** ZIP export as .txt: notes in the archive are now saved as plain text (.txt) with markdown stripped (headers, bold/italic, links, lists) via `stripMarkdown()`.
- **[RU]** Скачивание диалога в .txt: экспорт чата изменён с .md на .txt формат, подпись кнопки — «Скачать как текст».
- **[EN]** Dialogue download as .txt: chat export changed from .md to .txt format, button label — "Download as text".
- **[RU]** Удалена кнопка «Мой портрет» со страницы Профиля — портрет генерируется автоматически в фоне и доступен в Диагностике → Профиль AI.
- **[EN]** Removed "My Portrait" button from Profile page — the portrait is generated automatically in the background and available in Diagnostics → AI Profile.
- **[RU]** Авто-регенерация портрета: после фоновой пересуммаризации фасетов автоматически запускается `AIProfileService.generate()` — портрет больше не нужно обновлять вручную.
- **[EN]** Auto portrait regeneration: after background facet resummarization, `AIProfileService.generate()` is triggered automatically — the portrait no longer needs manual refresh.
- **[RU]** Портрет рендерится через MarkdownRenderer: в Диагностике текст портрета отображается как форматированный Markdown вместо сырого текста с символами `#` и `**`.
- **[EN]** Portrait rendered via MarkdownRenderer: in Diagnostics, the portrait text is displayed as formatted Markdown instead of raw text with `#` and `**` symbols.

## 2026-06-18 (v0.7.23)
- **[RU]** Гибридный поиск заметок (Vector + BM25 + RRF): векторный поиск теперь объединён с полнотекстовым поиском через MiniSearch (BM25 ранжирование, префиксы, нечёткий поиск с tolerate опечаток 0.2). Результаты сливаются по формуле Reciprocal Rank Fusion (RRF), давая топ-15 кандидатов для реранкинга. Точные даты, имена и термины теперь находятся даже при слабом векторном сходстве.
- **[EN]** Hybrid note search (Vector + BM25 + RRF): vector search is now combined with full-text search via MiniSearch (BM25 ranking, prefix matching, fuzzy 0.2 tolerance). Results are fused by Reciprocal Rank Fusion (RRF), yielding top-15 candidates for reranking. Exact dates, names and terms are now found even with weak vector similarity.
- **[RU]** Parent Document Retrieval: вместо загрузки всей заметки при совпадении по чанку, загружается только совпавший чанк и его соседи (±1) — экономит токены для длинных заметок.
- **[EN]** Parent Document Retrieval: instead of loading the full note when a chunk matches, only the matching chunk and its neighbours (±1) are loaded — saves tokens for long notes.
- **[RU]** Генеративное расширение поисковых запросов: перед поиском ИИ генерирует 3 альтернативных запроса (синонимы, связанные темы), которые тоже участвуют в поиске и сливаются через RRF — абстрактные запросы находят заметки, где исходные слова не упоминались.
- **[EN]** Generative query expansion: before search, the AI generates 3 alternative queries (synonyms, related topics) that also participate in search and are fused via RRF — abstract queries find notes where the original words weren't mentioned.
- **[RU]** Динамическое бюджетирование контекста: заметки добавляются в контекст ИИ по убыванию композитного балла (0.5×вектор + 0.3×ключевые слова + 0.2×свежесть), пока не достигнут лимит 25K символов — свежие и точные заметки приоритетнее старых с высоким векторным сходством.
- **[EN]** Dynamic context budgeting: notes are added to the AI context in descending order of composite score (0.5×vector + 0.3×keywords + 0.2×recency), until the 25K character budget is exhausted — fresh and keyword-matched notes take priority over old ones with high vector similarity.
- **[RU]** Управление объёмом ответа ИИ: переключатель «Кратко / Стандартно / Объёмно» в шапке каждого диалога — промпт модифицируется для соответствующего стиля ответа.
- **[EN]** AI response length control: a "Short / Standard / Detailed" toggle in each dialogue header — the prompt is modified to match the desired response style.
- **[RU]** Автоназвания диалогов: после первого ответа ИИ генерирует краткое название (3–5 слов) для диалога вместо обрезки первого сообщения. Кнопка «Переименовать» (карандаш) в шапке для ручного переименования.
- **[EN]** Auto-naming dialogues: after the first AI reply, a short title (3–5 words) is generated instead of truncating the first message. A "Rename" button (pencil) in the header allows manual renaming.
- **[RU]** Оптимистичный рендер сообщений: сообщение пользователя появляется мгновенно при отправке, а статус «{имя персоны} думает…» заменяет общий «печатает…» до начала стриминга.
- **[EN]** Optimistic message rendering: the user message appears instantly on send, and a "{persona name} thinking…" status replaces the generic "typing…" before streaming starts.
- **[RU]** Удалена персона «Журналист» — остались «Группа психологов», «КПТ-психолог», «Коуч», «Редактор».
- **[EN]** Removed "Journalist" persona — remaining: "Psychologist Panel", "CBT Therapist", "Coach", "Editor".
- **[RU]** Фасеты людей теперь строятся только из LLM-извлечения (mentionedPeople в саммари заметок) — убран regex-парсер с ложными совпадениями. В UI карточки разделены на «Сферы жизни и темы» и «Окружение (Люди)». Бейдж «в пост» на темах с высокой плотностью инсайтов + кнопка перехода к редактору с предзаполненным промптом.
- **[EN]** People facets now built exclusively from LLM extraction (mentionedPeople in note summaries) — removed the regex parser with false positives. UI cards are split into "Life spheres & themes" and "People". "To post" badge on themes with high insight density + button to jump to the editor with a pre-filled prompt.
- **[RU]** Облако слов в Архиве теперь кэшируется в localStorage и пересчитывается в фоне (при индексации/удалении заметок), а не при каждом рендере страницы — мгновенная загрузка без зависаний.
- **[EN]** Archive word cloud is now cached in localStorage and rebuilt in the background (on note indexing/deletion) instead of on every page render — instant load without freezes.
- **[RU]** Кнопка фильтрации в Архиве переименована с «теги» на «Теги/Метки».
- **[EN]** Archive filter button renamed from "tags" to "Tags/Labels".
- **[RU]** Улучшения качества: центроиды фасетов из реальных чанк-векторов (не сидов), взвешенный blend центроидов 1/(n+1) при инкременте, авто-ресуммаризация dirty фасетов (debounce 60с), кэш портрета между сообщениями, дедуп вызовов эмбеддингов, общий лимит контекста 30K, исправлена граница слов для кириллицы в поиске по именам.
- **[EN]** Quality improvements: facet centroids from actual chunk vectors (not seeds), weighted centroid blend 1/(n+1) on increment, auto-resummarization of dirty facets (60s debounce), portrait cache across messages, deduplicated embedding calls, 30K total context cap, fixed word boundary for Cyrillic in name search.
- **[RU]** Усилен промпт извлечения людей в `summarizeDocument` — только реальные имена, исключение персонажей/авторов/публичных деятелей. Промпт фасетов переведён на XML-теги `<label>/<description>` с fallback на старый формат.
- **[EN]** Strengthened people extraction prompt in `summarizeDocument` — only real names, excluding fictional characters/authors/public figures. Facet prompt switched to XML tags `<label>/<description>` with fallback to legacy format.

## 2026-06-17 (v0.7.22)
- **[RU]** Профиль: фасеты описывает послушная модель. Активная чат-модель (DeepSeek v4) — reasoning-модель и сыплет рассуждения в ответ («Мы должны проанализировать… похоже, жену зовут…») независимо от формата. Теперь `summarizeFacet` зовёт отдельную модель (`AI_FACET_MODEL`, по умолч. Qwen3 30B-A3B) через новый оверрайд `model` в `generate`. Парсер срезает преамбулу (берёт текст после «ОПИСАНИЕ:») и жёстко отклоняет любые рассуждения → в карточку попадает либо чистое описание, либо чистые ключевые слова, но не «сырьё мышления».
- **[EN]** Profile: facets summarized by an obedient model. The active chat model (DeepSeek v4) is a reasoning model that leaks its chain-of-thought into the answer regardless of format. `summarizeFacet` now calls a dedicated model (`AI_FACET_MODEL`, default Qwen3 30B-A3B) via a new `model` override in `generate`. The parser strips any preamble (takes text after "ОПИСАНИЕ:") and hard-rejects reasoning → a card shows a clean summary or clean keywords, never raw thinking.
- **[RU]** Профиль: надёжность ИИ-описаний фасетов. `summarizeFacet` переведён с жёсткого JSON-режима (DeepSeek на нём регулярно срывался в рассуждения → отклонялось → мусорный keyword-фолбэк) на простой формат `НАЗВАНИЕ:/ОПИСАНИЕ:` с устойчивым парсером (если формат проигнорён, но текст чистый русский — берётся как описание). Должно резко снизить число фасетов, упавших в фолбэк.
- **[EN]** Profile: reliable AI facet summaries. `summarizeFacet` switched from strict JSON mode (DeepSeek kept slipping into reasoning → rejected → junk keyword fallback) to a simple `НАЗВАНИЕ:/ОПИСАНИЕ:` format with a resilient parser (clean Russian prose is accepted even if the format is ignored). Should sharply cut the number of facets that fall back.
- **[RU]** Профиль: убран шум и сырой текст из карточек. Фолбэк (когда ИИ не справился) больше **не показывает сырой фрагмент заметки** — только чистые ключевые слова. «Прочие» темы, которые ИИ не смог осмысленно назвать, или которые оказались свалкой (>40% всех заметок — обычно приветствия/логистика), теперь **отбрасываются**. Жизненные домены остаются всегда (с keyword-фолбэком, если ИИ не дал описание).
- **[EN]** Profile: removed noise and raw text from cards. The fallback (when the AI failed) no longer shows a **raw note fragment** — only clean keywords. "Discovered" themes the AI couldn't meaningfully name, or that became a catch-all (>40% of all notes — usually greetings/logistics), are now **dropped**. Life-domains are always kept (with a keyword fallback if the AI gave no summary).
- **[RU]** Профиль: chunk-level домены (точность тем). Теперь каждый **кусок** заметки назначается в свой домен (а не вся заметка целиком), и домен описывается ИИ **по своим кускам**. Это разделяет смешанные дневниковые записи: «денежные» абзацы идут в «Деньги», «про жену» — в «Партнёра», даже если они в одной заметке. `embedDocument` отдаёт тексты чанков, они хранятся (схема эмбеддингов v3 — нужен реиндекс), назначение по chunk-cosine (порог 0.45), описание фасета строится из его фрагментов с фокусом по теме. Фолбэк тоже из релевантных фрагментов. Кнопка «Переиндексировать всё» в «Базе данных» обновит формат.
- **[EN]** Profile: chunk-level domains (theme accuracy). Each note CHUNK is now assigned to its own domain (instead of the whole note), and a domain is summarized by the AI from ITS OWN chunks. This separates mixed journal entries: "money" paragraphs go to Деньги, "wife" paragraphs to Партнёр, even within one note. `embedDocument` returns chunk texts which are stored (embedding schema v3 — needs a re-index), assignment is by chunk cosine (threshold 0.45), and the facet summary is built from its fragments with a domain focus. Fallback also uses the relevant fragments. The "Reindex all" button in Database updates the format.
- **[RU]** Профиль: заданные жизненные домены. Темы строятся из канонических доменов (Деньги, Отношения с детьми/партнёром/родителями/коллегами, Самореализация) — заметка попадает в домен по близости её чанков к эмбеддингу-сид домена; заметки вне доменов кластеризуются в «прочие» темы (так сохраняются, напр., сценарии про котят). Карточки темы раскрываются по клику (полный текст) + кнопка «Выгрузить» (копирует все темы в буфер markdown). Промпт фасетов ужесточён (строго русский, без мета-рассуждений), название выводится из описания, если модель его не дала. Конкретные люди (отец/мать/жена/дети) — отдельным фасетом на Фазе 2+.
- **[EN]** Profile: seeded life-domains. Themes are built from canonical domains (Money, relationships with children/partner/parents/colleagues, Self-realization) — a note joins a domain when its chunks are close to the domain's seed embedding; notes outside all domains are clustered into "discovered" themes (so e.g. the kids'-stories cluster survives). Theme cards expand on click (full text) + a "Export" button (copies all themes as markdown). Facet prompt hardened (Russian only, no meta-narration), label derived from the summary when the model omits it. Specific people (father/mother/spouse/children) come as their own facet in Phase 2+.
- **[RU]** Живой AI-профиль, Фаза 1 — темы (фасеты). Заметки кластеризуются по смыслу (косинус-k-means по чанк-векторам, локально в браузере; заметка может попадать в несколько тем), и для каждой темы ИИ пишет описание (новый лимит-экемпт эндпоинт `summarizeFacet`). В Диагностике → «Профиль AI» появилась секция «Темы профиля»: карточки с названием, описанием, числом заметок, диапазоном дат и бейджем тренда (новая/активна/затихла), кнопка «Построить/Перестроить». Новый стор `aiProfileFacets` (схема БД v7). Дальше — инкрементальное обновление и связка с чатом.
- **[EN]** Living AI profile, Phase 1 — themes (facets). Notes are clustered by meaning (cosine k-means over chunk vectors, client-side; a note can belong to several themes) and the AI writes a description per theme (new limit-exempt `summarizeFacet` endpoint). Diagnostics → "AI Profile" gains a "Profile themes" section: cards with label, description, note count, date range and a trend badge (new/active/faded), plus a Build/Rebuild button. New `aiProfileFacets` store (DB schema v7). Next: incremental updates and chat integration.
- **[RU]** «Липкий» поиск в чате: после поискового запроса следующие 4 реплики тоже подтягивают заметки, даже если не похожи на поиск («посмотри в этих», «собери», «а ещё») — переиспользуется последний реальный запрос, чтобы тема не терялась. Сбрасывается при смене диалога. Плюс кнопка «Переиндексировать всё» в Диагностике (форс-пересчёт всех заметок, с подтверждением) — на случай смены алгоритма/модели или для подстраховки.
- **[EN]** "Sticky" in-chat search: after a search query, the next 4 turns also pull notes even if they don't look like a search ("посмотри в этих", "собери", "а ещё") — reusing the last real query so the topic isn't lost. Resets on dialogue switch. Plus a "Reindex all" button in Diagnostics (forced re-embed of every note, with confirmation) — for algorithm/model changes or peace of mind.
- **[RU]** Глобальный запрет на выдумывание заметок (`NOTES_GUARD`) во всех персонах чата (Cloud Functions + `/api/chat`): модель не пересказывает/не цитирует/не реконструирует содержание заметок, если их текста нет в контексте диалога — вместо сочинительства честно говорит, что заметок нет перед глазами. Лечит «странные диалоги», где ассистент уверенно выдумывал содержание перечисленных заметок в репликах без поиска. Триггеры поиска расширены (посмотри/проверь/глянь/собери/подбери/покажи/перепроверь).
- **[EN]** Global anti-fabrication rule (`NOTES_GUARD`) across all chat personas (Cloud Functions + `/api/chat`): the model won't retell/quote/reconstruct note content that isn't present in the dialogue context — instead of inventing it honestly says it doesn't have the notes in front of it. Fixes the "weird dialogs" where the assistant confidently fabricated the content of listed notes in non-search turns. Search triggers widened (посмотри/проверь/глянь/собери/подбери/покажи/перепроверь).
- **[RU]** Поиск в чате больше не проваливается молча в ответ «у меня нет доступа к заметкам». Сборка карточек для рерэнка читала саммари из облака, и при заблокированном E2E `maybeDecrypt` бросал `LOCKED` → весь `searchNotes` падал, контекст не подмешивался, и персона отвечала как без поиска. Теперь карточки читаются только из локальной базы с `try/catch` (нет саммари — плейсхолдер), а если поиск всё же падает — в контекст идёт guard «поиск не сработал, не выдумывай», а не пустота.
- **[EN]** In-chat search no longer silently falls back to "I have no access to your notes". Rerank-card building read summaries from the cloud, and with E2E locked `maybeDecrypt` threw `LOCKED` → all of `searchNotes` failed, no context was injected, and the persona answered as if no search ran. Cards are now read local-only with `try/catch` (missing summary → placeholder), and if search still fails a "search failed, don't fabricate" guard is injected instead of nothing.

## 2026-06-16 (v0.7.21)
- **[RU]** Качество ответов поиска в чате. Запрос-поиск определяется регексом, а не фиксированным списком фраз — ловит «где про X пишу», уточняющие follow-up'ы и т.п. (не срабатывает на «напиши …»). Контекст подаётся как «результаты автопоиска по архиву», а не одна прикреплённая заметка — персона больше не отвечает «у меня нет доступа к заметкам». Добавлен запрет на выдумывание: модель опирается ИСКЛЮЧИТЕЛЬНО на найденные заметки, а при пустом поиске честно говорит «не нашёл» (фикс галлюцинаций, когда выдумывались несуществующие факты).
- **[EN]** In-chat search answer quality. Search intent is detected by regex instead of a fixed phrase list — catches "где про X пишу", follow-ups, etc. (and not the imperative "напиши …"). Context is framed as "automatic archive search results", not a single attached note — the persona no longer claims "I have no access to your notes". Added an anti-fabrication guard: the model uses ONLY the retrieved notes and honestly says "not found" on an empty search (fixes hallucinated, non-existent facts).
- **[RU]** В выборе заметки для разбора («Какую заметку разберём?») теперь видны ВСЕ заметки. Раньше грузились только 20 последних и поиск фильтровал внутри них, поэтому старые заметки не находились по названию. Теперь грузится весь список (одним bulk-чтением версий), поиск идёт по всему набору.
- **[EN]** The note picker ("Какую заметку разберём?") now shows ALL notes. It previously loaded only the 20 most recent and filtered within them, so older notes were unsearchable by title. It now loads the full list (one bulk versions read) and searches the whole set.
- **[RU]** Чанкинг эмбеддингов (recall на беглых упоминаниях). Раньше на заметку был один усреднённый вектор — краткое упоминание (напр. имени) в длинной заметке «размывалось» и заметка не находилась. Теперь заметка режется на перекрывающиеся куски (~1000 симв, overlap 150, до 40), на каждый — свой вектор; релевантность заметки = лучший косинус среди её кусков (`topKMulti`, max-pool). `embedDocument` возвращает векторы по кускам, схема эмбеддингов v2 (`schemaV`) форсирует полный реиндекс, чтение старого одно-векторного формата сохранено для совместимости. Top-K кандидатов 15→40. Один embed-вызов на заметку (Fireworks принимает массив), стоимость та же.
- **[EN]** Embedding chunking (recall for brief mentions). Previously one mean-pooled vector per note diluted a short mention (e.g. a name) in a long note, so it wasn't found. Notes are now split into overlapping chunks (~1000 chars, 150 overlap, up to 40), each with its own vector; a note's relevance = best cosine across its chunks (`topKMulti`, max-pool). `embedDocument` returns per-chunk vectors, embedding schema v2 (`schemaV`) forces a full re-index, legacy single-vector reads kept for compat. Candidate top-K 15→40. One embed call per note (Fireworks accepts arrays), same cost.
- **[RU]** Поиск по заметкам в чате больше не валит запрос (400). Найденные заметки подмешивались в текст сообщения, но у сообщения лимит 10K символов, а 5 заметок его пробивали → `/api/chat` отвечал `400 Bad Request`. Теперь контекст поиска уходит в поле `documentContent` (лимит 50K), сниппет на заметку ограничен 6K, общий объём — 45K. Сообщение остаётся компактным.
- **[EN]** In-chat note search no longer breaks the request (400). Retrieved notes were appended to the message text, but a message is capped at 10K chars and 5 notes overflowed it → `/api/chat` returned `400 Bad Request`. Search context now goes through the `documentContent` field (50K cap), per-note snippet limited to 6K, total to 45K. The message stays compact.
- **[RU]** Персональный дневной лимит ИИ для админа: пользователи с ролью `admin` получают `AI_ADMIN_DAILY_LIMIT` (по умолчанию 100) вместо общих 5/сутки. Применяется в обеих точках контроля (`chatWithAI`/Cloud Functions и `/api/chat`/Vercel) и в отображении лимита (`getAILimit`). Обычные пользователи — без изменений.
- **[EN]** Per-user daily AI cap bump for admins: users with role `admin` get `AI_ADMIN_DAILY_LIMIT` (default 100) instead of the shared 5/day. Applied at both enforcement points (`chatWithAI`/Cloud Functions and `/api/chat`/Vercel) and in the limit display (`getAILimit`). Regular users unchanged.
- **[RU]** Семантический поиск больше не жжёт чат-квоту. LLM-рерэнк кандидатов раньше шёл через `chatWithAI` (лимит 5/сутки + кулдаун 10 с) — каждый поиск ловил `429` и тратил лимит чата. Вынес рерэнк в отдельный лимит-экемпт эндпоинт `rerankNotes` (только глобальный кап расходов, как у эмбеддингов); `noteRetriever` зовёт его вместо чата, при любой ошибке — мягкий фолбэк на чистый векторный top-K. Заодно убран баг с `personaId`.
- **[RU]** Эмбеддинги выведены из-под пользовательских AI-лимитов: `embedDocument` больше не дёргает `checkDailyLimit` (5/сутки) и `checkRateLimit` (кулдаун 10 с) — из-за них массовая индексация ловила `429 Too Many Requests` и упиралась в 5 заметок в день, плюс это ело лимит чата. Осталась только защита расходов проекта (`withinGlobalDailyLimit`). В клиентском прогоне добавлен мягкий интервал 150 мс между реальными эмбеддингами. Функция перезадеплоена.
- **[RU]** Облачный до-синк эмбеддингов: записи с флагом `cloudSyncedAt` не получившие облака (например, посчитанные при заблокированном E2E) теперь догружаются — фоновым проходом индексатора и кнопкой «В облако» в Диагностике (без вызовов ИИ, чистые записи в Firestore). В панель добавлен счётчик «Не в облаке».
- **[RU]** Индексация эмбеддингов больше не падает на облаке. Добавлено правило Firestore для `users/{uid}/embeddings/{docId}` (читать/писать только владельцу) — без него запись/чтение давали `Missing or insufficient permissions` и ломали фоновый прогон. Проверка свежести в индексаторе теперь идёт только по локальной базе (не дёргает облако в цикле, где чтение могло бросить permissions или `LOCKED` при заблокированном E2E). Облачное чтение/запись эмбеддингов стали best-effort: ошибки не валят локальную индексацию, а `ENCRYPT_REQUIRED` (E2E заблокирован) логируется тихо — эмбеддинг сохраняется локально и синхронизируется позже. Правила задеплоены на обе БД.
- **[EN]** Semantic search no longer burns the chat quota. The candidate LLM rerank previously went through `chatWithAI` (5/day cap + 10s cooldown), so every search hit `429` and consumed chat quota. Moved rerank to a dedicated limit-exempt endpoint `rerankNotes` (project cost guard only, like embeddings); `noteRetriever` calls it instead of chat and falls back to plain vector top-K on any error. Also removed the stale `personaId` hack.
- **[EN]** Embeddings are no longer subject to user-facing AI limits: `embedDocument` no longer calls `checkDailyLimit` (5/day) or `checkRateLimit` (10s cooldown) — they caused `429 Too Many Requests` and capped bulk indexing at 5 notes/day while also eating the chat quota. Only the project-wide cost guard (`withinGlobalDailyLimit`) remains. The client run adds a gentle 150ms gap between real embed calls. Function redeployed.
- **[EN]** Embedding cloud back-sync: records without `cloudSyncedAt` (e.g. computed while E2E was locked) are now uploaded — by a background indexer pass and a "To cloud" button in Diagnostics (no AI calls, plain Firestore writes). Added an "Unsynced" counter to the panel.
- **[EN]** Embedding indexing no longer breaks on the cloud path. Added a Firestore rule for `users/{uid}/embeddings/{docId}` (owner read/write) — without it writes/reads returned `Missing or insufficient permissions` and broke the background run. The indexer's freshness check is now local-only (no cloud read in the loop, where it could throw permissions or `LOCKED` when E2E is locked). Cloud read/write of embeddings is best-effort: failures no longer break local indexing, and `ENCRYPT_REQUIRED` (E2E locked) is logged quietly — the embedding is stored locally and syncs later. Rules deployed to both databases.

## 2026-06-16 (v0.7.20)
- **[RU]** Service worker больше не оставляет пользователя на старом бандле после деплоя. Навигация переведена на network-first (свежий `index.html` всегда при онлайне, кэш — только офлайн-резерв), `CACHE_VERSION` бампнут до `v0.7.20` (старые кэши вычищаются в `activate`). Это устраняло баг, всплывший на бампе IndexedDB до v6: старый кэшированный shell тянул старый JS, который не мог открыть обновлённую базу, и UI выглядел как «заметки пропали» (данные при этом были целы). Хешированные `/assets/` остаются cache-first (имена контент-хешированы). Гейт: prod-сборка зелёная.
- **[EN]** The service worker no longer strands users on a stale bundle after a deploy. Navigation is now network-first (fresh `index.html` whenever online, cache only as an offline fallback) and `CACHE_VERSION` was bumped to `v0.7.20` (old caches purged in `activate`). This fixes the bug surfaced by the IndexedDB v6 bump: a stale cached shell loaded old JS that could not open the upgraded DB, so the UI looked like "notes disappeared" (the data was always intact). Hashed `/assets/` stay cache-first (content-hashed names). Gate: production build green.

## 2026-06-16 (v0.7.19)
- **[RU]** Семантический поиск по заметкам через эмбеддинги. Чтобы запросы вида «что я писал про X / что годится в пост» не грузили весь корпус в LLM, каждая заметка индексируется одним вектором (Fireworks `qwen3-embedding-8b`, 1024 dim — мультиязычная, для русского) и ищется клиентски (косинус в браузере). Двухстадийно: эмбед запроса → top-K локально → дешёвый LLM-рерэнк по саммари-карточкам → полный текст только для финальных заметок. Векторы хранятся локально (IndexedDB стор `aiEmbeddings`, схема v6 — аддитивная миграция) и зашифрованно в облаке (E2E, как саммари). Новая Cloud Function `embedDocument` (Fireworks `/embeddings`, лимиты как у `summarizeDocument`). Фоновый индексатор (`useEmbeddingIndexer`) считает эмбеддинги инкрементально при простое+онлайн, пересчёт по `contentHash`/смене модели или размерности.
- **[RU]** Инструменты наблюдаемости в Диагностике → «База данных»: панель статуса индексации (покрытие корпуса, число проиндексированных/ожидающих, модель/размерность, время последней) с кнопкой «Индексировать сейчас» (форс-прогон с прогрессом) и тестовым поиском по заметкам (запрос → топ-5 с релевантностью); стор `aiEmbeddings` добавлен в обозреватель таблиц; в «Систему» добавлен счётчик эмбеддингов и версия локальной БД исправлена на 6. Логика индексации вынесена в `embeddingIndexer.ts` (общий код для фонового хука и ручного триггера). Гейт: `tsc` 0 (root + functions), ESLint 0, vitest (vectorSearch 8/8), прод-сборка зелёная.
- **[EN]** Semantic note search via embeddings. So that queries like "what did I write about X / what could become a post" don't load the whole corpus into the LLM, each note is indexed by a single vector (Fireworks `qwen3-embedding-8b`, 1024 dim — multilingual, for Russian) and searched client-side (cosine in the browser). Two-stage: embed the query → top-K locally → cheap LLM rerank over summary cards → full text only for the final notes. Vectors are stored locally (IndexedDB `aiEmbeddings` store, schema v6 — additive migration) and encrypted in the cloud (E2E, like summaries). New `embedDocument` Cloud Function (Fireworks `/embeddings`, same limits as `summarizeDocument`). A background indexer (`useEmbeddingIndexer`) computes embeddings incrementally on idle + online, recomputing on `contentHash`/model/dimension change.
- **[EN]** Observability tools in Diagnostics → "Database": an indexing-status panel (corpus coverage, indexed/pending counts, model/dimension, last run) with an "Index now" button (forced run with progress) and a test note search (query → top-5 with relevance scores); the `aiEmbeddings` store was added to the table explorer; the "System" tab gained an embeddings counter and the local DB version was corrected to 6. Indexing logic extracted to `embeddingIndexer.ts` (shared by the background hook and the manual trigger). Gate: `tsc` 0 (root + functions), ESLint 0, vitest (vectorSearch 8/8), production build green.

## 2026-06-12 (v0.7.18)
- **[RU]** Исправлено нарушение CSP `script-src` при открытии заметки из архива и нажатии «Продолжить писать». Чанк назывался `vendor-ai`, но AI SDK не при чём — внутри `zod` v4 (4.3.6): его JIT-парсер пробует `new Function("")` для feature-detection, что наша CSP (без `unsafe-eval`) блокирует и логирует как ошибку. Всплывало именно на этом пути, т.к. там первый JIT-пригодный `safeParse` (`VersionService.getLatestVersion` → `versionDbSchema`). Отключил JIT через `z.config({ jitless: true })` в `main.tsx` — короткое замыкание не доходит до `new Function`, парсинг идёт интерпретатором, CSP остаётся жёсткой. Гейт: `tsc` 0, прод-сборка зелёная.
- **[EN]** Fixed a CSP `script-src` violation when opening a note from the archive and clicking "Continue writing". The chunk was named `vendor-ai`, but the AI SDK is unrelated — it actually contains `zod` v4 (4.3.6): its JIT parser probes `new Function("")` for feature detection, which our CSP (no `unsafe-eval`) blocks and logs as an error. It surfaced on this path because it holds the first JIT-eligible `safeParse` (`VersionService.getLatestVersion` → `versionDbSchema`). Disabled the JIT via `z.config({ jitless: true })` in `main.tsx` — the short-circuit never reaches `new Function`, parsing uses the interpreter, and the CSP stays hardened. Gate: `tsc` 0, production build green.
- **[RU]** «Продолжить писать» из архива снова работает. Раньше всплывал тост «Ошибка при продолжении сессии»: `handleContinueSessionOrDoc` обрабатывал только `LifeLogDocument` (путь лайфлога), а архив передаёт `ArchiveSession` — попадало в `else` и сразу слало ошибку, ветку для архива не доделали. Теперь `ArchiveSession` конвертируется в `LifeLogDocument` (флаги хранения `_isLocal`/`_linkedCloudId` → `localId`/`cloudId`) и проходит через тот же `handleContinueDocument`: локальная заметка грузится из локального хранилища, облачная подтягивает локальную копию. Чинит и десктоп, и мобайл-архив.
- **[EN]** "Continue writing" from the archive works again. It used to show an "Error continuing session" toast: `handleContinueSessionOrDoc` only handled `LifeLogDocument` (the LifeLog path), while the archive passes an `ArchiveSession` — it fell into the `else` and raised an error, as the archive branch was never finished. Now the `ArchiveSession` is mapped to a `LifeLogDocument` (storage flags `_isLocal`/`_linkedCloudId` → `localId`/`cloudId`) and goes through the same `handleContinueDocument`: a local note loads from local storage, a cloud note pulls a local copy. Fixes both desktop and mobile archive.

## 2026-06-10 (v0.7.17)
- **[RU]** Фиксы сохранения и архива. **Зависание сохранения после выбора эмоции:** модалка завершения висла с вечным спиннером — `useServiceAction` ставил `abortedRef = true` в cleanup эффекта и никогда не сбрасывал на маунте; в StrictMode (mount → cleanup → mount) хук навсегда оставался «aborted» и молча глотал `onSuccess`/`setIsLoading(false)` (заметка при этом реально сохранялась — UI просто не узнавал). Добавлен сброс флага при монтировании. **Правила Firestore:** в whitelist `isValidVersion.hasOnly()` отсутствовал ключ `mood` — запись версии с эмоцией у залогиненных отклонялась с `permission-denied`; добавлен `mood` (string ≤ 50), правила задеплоены на обе БД (`(default)` и `ai-studio-…`; задеплоенная версия была устаревшей и отклоняла даже автосейв черновиков). **Чек-ин настроения:** 5 смайлов заменены на 6 базовых эмоций (радость/грусть/злость/страх/отвращение/интерес), ключи переводов обновлены. **Попап меток:** список обрезался скролл-контейнером архива (`absolute` внутри `overflow-y-auto`/Virtuoso) — вынесен в portal на `document.body` с `position: fixed`, ограничением высоты и закрытием при скролле/ресайзе (NoteRow + GridNoteCard); пункты выровнены влево — в NoteRow не было `justify-start`, а базовый `Button` центрирует контент. **Архив:** бары тегов/меток скрыты по умолчанию, добавлена кнопка-тогл «теги» в шапке; карточки в сетке показывают время сессии (`sessionStartTime`), а не дату документа. **Модалка завершения:** «Вернуться» читает статус/контент напрямую из стора (раньше — устаревшее замыкание, пауза не возобновлялась). **i18n:** добавлены недостающие ключи `app_name` и `close` (логотип и кнопки закрытия логировали Missing translation key). Гейт: `tsc` 0, ESLint 0, vitest зелёный.
- **[EN]** Save and archive fixes. **Save hang after picking an emotion:** the finish modal spun forever — `useServiceAction` set `abortedRef = true` in the effect cleanup and never reset it on mount; under StrictMode (mount → cleanup → mount) the hook stayed permanently "aborted", silently swallowing `onSuccess`/`setIsLoading(false)` (the note was actually saved — the UI just never learned). Flag is now re-armed on mount. **Firestore rules:** the `isValidVersion.hasOnly()` whitelist was missing the `mood` key — version writes with an emotion were rejected with `permission-denied` for signed-in users; added `mood` (string ≤ 50) and deployed the rules to both databases (`(default)` and `ai-studio-…`; the deployed version was stale and even rejected draft autosaves). **Mood check-in:** 5 smileys replaced with 6 basic emotions (joy/sadness/anger/fear/disgust/interest), translation keys updated. **Label picker:** the popup was clipped by the archive scroll container (`absolute` inside `overflow-y-auto`/Virtuoso) — moved to a portal on `document.body` with `position: fixed`, height clamping and close-on-scroll/resize (NoteRow + GridNoteCard); items are left-aligned — NoteRow lacked `justify-start` while the base `Button` centers content. **Archive:** tag/label bars hidden by default with a new "tags" toggle in the header; grid cards show the session time (`sessionStartTime`) instead of the document date. **Finish modal:** "Back" reads status/content directly from the store (previously a stale closure, so a paused session didn't resume). **i18n:** added the missing `app_name` and `close` keys (the logo and close buttons logged Missing translation key). Gate: `tsc` 0, ESLint 0, vitest green.

## 2026-06-04 (v0.7.16)
- **[RU]** Волна багфиксов + UX-улучшения. **FlowPulse (пульсация внизу при наборе):** анимация сломалась в v0.7.12 при миграции на Tailwind-классы — `ease-[ease,cubic-bezier(...)]` и `duration-[0.6s,1.2s]` с запятыми в arbitrary-value не генерировали корректный multi-value CSS; возврат к inline-стилям плюс чуть увеличена высота (2→3px) и яркость. **AI-чат — дублирование сообщений:** при отправке второго и последующих сообщений `sendMessage` строил `allMessages` из `optimisticDialogue` (который уже содержал новое сообщение) и добавлял его ещё раз — ИИ получал текст дважды; особенно заметно при прикреплении заметки. Исправлено: `allMessages` строится из оригинального `dialogue` до оптимистичного обновления. **Мобильный режим:** v0.7.11 случайно реинтродуцировал `mediaQuery` listener (480px breakpoint), удалённый ещё в коммите `5d3d69a` («layoutMode manual-only»); убран, ключ localStorage сменён на `layout-mode-v3` — все пользователи, застрявшие в mobile, получат `desktop` при следующей загрузке. **Горячая клавиша Cmd+M:** в v0.7.1 добавился лишний `e.shiftKey`, превративший шорткат в `Cmd+Shift+M`; возвращено `Cmd+M` / `Ctrl+M`. **Поле названия:** при фокусе текст становится ярче (opacity 50→100%) и появляется мягкое рассеянное свечение (`box-shadow 0 0 25px brand-soft 15%`); работает в обоих режимах (обычном хедере и LifeLog-хедере). **Модели (Admin):** в переключатель и allow-list `setAIModel` добавлены Qwen3 30B A3B и Qwen3.6 Plus (Fireworks) для A/B-сравнения с DeepSeek. Гейт: `tsc` 0, ESLint 0, vitest 523.
- **[EN]** Bug-fix wave + UX improvements. **FlowPulse (bottom pulse on typing):** animation broke in v0.7.12 during migration to Tailwind classes — `ease-[ease,cubic-bezier(...)]` and `duration-[0.6s,1.2s]` with commas in arbitrary-value did not generate valid multi-value CSS; reverted to inline styles, also bumped height 2→3px and brightness. **AI chat — message duplication:** when sending the second and later messages, `sendMessage` built `allMessages` from `optimisticDialogue` (which already contained the new message) and appended it a second time — the AI received the text twice; especially obvious when attaching a note. Fixed: `allMessages` is built from the original `dialogue` before the optimistic update. **Mobile layout mode:** v0.7.11 accidentally re-introduced a `mediaQuery` listener (480px breakpoint) that had been intentionally removed in commit `5d3d69a` ("layoutMode manual-only"); removed again, storage key bumped to `layout-mode-v3` — all users stuck on mobile get `desktop` on next load. **Cmd+M shortcut:** v0.7.1 accidentally added `e.shiftKey`, turning the shortcut into `Cmd+Shift+M`; restored to `Cmd+M` / `Ctrl+M`. **Title input:** text brightens on focus (opacity 50→100%) with a soft ambient glow (`box-shadow 0 0 25px brand-soft 15%`); works in both modes (standard header and LifeLog header). **Models (Admin):** added Qwen3 30B A3B and Qwen3.6 Plus (Fireworks) to the model switcher and `setAIModel` allow-list for A/B comparison with DeepSeek. Gate: `tsc` 0, ESLint 0, vitest 523.

## 2026-06-03 (v0.7.15)
- **[RU]** Переход на Fireworks AI (DeepSeek) + переключатель модели в админке + детальная статистика запросов. **Провайдер:** добавлен шов `aiProvider.ts` — единый `generate()`, маршрутизирующий запросы на Fireworks (OpenAI-совместимый `/chat/completions`) или Gemini в зависимости от `AI_PROVIDER`; стриминг-чат (`/api/chat`) аналогично переключается через `getChatModel()`. Все 4 Cloud Functions (chat / summarize / edit / validate) и Vercel streaming chat мигрированы на шов. **Модель по умолчанию — DeepSeek v4 Flash** (`accounts/fireworks/models/deepseek-v4-flash`): в **12 раз дешевле** DeepSeek v4 Pro ($0.14/$0.28 vs $1.74/$3.48 за M токенов), без паузы на reasoning, тот же уровень качества для задач приложения (чат с персонами, саммари, редактура русских текстов). **Динамическое переключение:** модель хранится в Firestore (`appConfig/ai`) и читается с 60-секундным in-memory кешем; новые Cloud Functions `setAIModel` и `getAIConfig` (только для admin); UI-переключатель в Диагностике → Статистика AI — три кнопки: Flash / Pro / GPT OSS 120B — вступают в силу без перезапуска. **Детальная статистика:** `recordUsage` теперь дополнительно пишет per-request событие (`aiUsage/{uid}/events/{id}`) с полями `ts, tokensIn, tokensOut, model, fn`; в таблице пользователей кликабельные строки раскрывают список запросов за выбранный день. **Таблица тарифов:** актуальные цены трёх моделей из официальных источников (docs.fireworks.ai, ai.google.dev) с колонкой ориентировочной стоимости запроса. Гейт: `tsc` 0 (functions + root), ESLint 0, vitest 525, Vite prod build OK, Functions build OK.
- **[EN]** Switch to Fireworks AI (DeepSeek) + admin model switcher + per-request stat breakdown. **Provider seam:** added `aiProvider.ts` — a single `generate()` that routes to Fireworks (OpenAI-compatible `/chat/completions`) or Gemini depending on `AI_PROVIDER`; the streaming chat (`/api/chat`) switches similarly via `getChatModel()`. All 4 Cloud Functions (chat / summarize / edit / validate) and the Vercel streaming chat are migrated to the seam. **Default model: DeepSeek v4 Flash** (`accounts/fireworks/models/deepseek-v4-flash`): **12× cheaper** than DeepSeek v4 Pro ($0.14/$0.28 vs $1.74/$3.48 per M tokens), no reasoning delay, same quality level for the app's tasks (persona chat, summarization, Russian text editing). **Dynamic switching:** the active model is stored in Firestore (`appConfig/ai`) and read with a 60-second in-memory cache; new `setAIModel` and `getAIConfig` Cloud Functions (admin-only); a UI switcher in Diagnostics → AI Stats — three buttons: Flash / Pro / GPT OSS 120B — takes effect without a restart. **Detailed stats:** `recordUsage` now also writes a per-request event (`aiUsage/{uid}/events/{id}`) with `ts, tokensIn, tokensOut, model, fn` fields; user rows in the stats table are now expandable to show each request for the selected day. **Pricing table:** up-to-date rates for three models from official sources (docs.fireworks.ai, ai.google.dev) with an estimated cost-per-request column. Gate: `tsc` 0 (functions + root), ESLint 0, vitest 525, Vite prod build OK, Functions build OK.

## 2026-06-03 (v0.7.14)
- **[RU]** Фиксы по итогам прод-тестирования. **Синхронизация (диагностика):** в таблицу добавлена колонка **«Дата»** (из `lastSessionAt`/`firstSessionAt`), **сортировка по любой колонке** (клик по заголовку, переключение asc/desc) и **поиск по названию** заметки — и в десктоп-таблице, и в мобильных карточках. **Тег (косметика):** чип добавленного тега раздувался до ~36px, потому что крестик «×» мигрировал на `<IconButton>` с дефолтным `size="md"` (`w-9 h-9`) без override — добавлен `w-auto h-auto`, чип снова по размеру кнопки «+ тег». **AI-чат:** при сбое стрима Gemini (например, упёрся в spend-cap) поток закрывался пустым без исключения → сохранялся пустой пузырь; теперь пустой ответ перехватывается и показывается понятная ошибка. **AI-саммари (`summarizeDocument`):** провалившиеся запросы больше не жгут дневной лимit (`refundDailyLimit` в catch), реальная ошибка Gemini теперь логируется в консоль функции (раньше уходила только в Langfuse → «нет логов»), а ошибка квоты/spend-cap мапится в понятный `resource-exhausted`. *Примечание: первопричина недоступности AI на проде — месячный spend cap проекта Gemini (AI Studio), это настройка биллинга, не код.* Гейт зелёный: ESLint 0, `tsc` 0, vitest 525, прод-сборка Vite + prerender, сборка Cloud Functions.

## 2026-06-03 (v0.7.13)
- **[RU]** Завершение миграции на дизайн-систему (T4/T5) + волна багфиксов по итогам смоук-тестов. **Дизайн-система:** raw `<button>`/`<input>`/`<textarea>` переведены на примитивы `<Button>`/`<IconButton>`/`<Input>`/`<Textarea>` (a11y: `type="button"`, `aria-label`, focus-visible; единый вид). Bespoke-элементы (редакторы, инлайн-теги/метки, большие number-инпуты сетапа) оставлены raw — примитивы ломали их лейаут. **Примитив Button:** расширен конфиг `tailwind-merge` (`text-label`/`text-label-sm` зарегистрированы как font-size) — кастомные размеры шрифта больше не перебиваются дефолтным `text-sm`. **Грид заметок (prod-краш):** `<Virtuoso data={sortedDates}>` с вложенными card-гридами переменной высоты рассинхронизировался при смене сортировки/фильтра/вида → `Cannot read 'length' of undefined`, фантомные пустые группы дат и пропавшие карточки; сгруппированная сетка переписана без виртуализации (+ пропуск пустых групп). **AI на карточке:** кнопка «AI» по первому клику генерирует саммари (`AIService.summarize` → `AISummaryService.save`, спиннер, портрет профиля), повторный — открывает чат. **setState-in-render:** в `useLocalStorage` побочные эффекты (`localStorage.setItem` + `dispatchEvent`) вынесены из апдейтера состояния — устранён поток React-варнингов «Cannot update a component while rendering…» (спамил ~20× на экране письма). **Точечные фиксы:** пикер меток не разъезжается на длинных именах (`whitespace-nowrap`); индикатор метки не схлопывается в полоску (утечка `px-4` в `w-7 h-7`-кнопку → `p-0`/`shrink-0`, + 3 аналогичные squish-кнопки); лейблы навигации в сайдбаре слева, футер переупорядочен (Настройки → О приложении → версия). Полный гейт зелёный: ESLint 0, `tsc` 0, vitest 525, прод-сборка Vite + prerender, сборка Cloud Functions.

## 2026-06-02 (v0.7.12)
- **[RU]** Остаточный техдолг по аудиту + важный багфикс. **Фикс (prod):** в мобильном меню действий с заметкой каждое действие отображалось дважды — миграция кнопок в v0.7.11 добавила новые `<Button>`, но не удалила старые `<button>`-дубли (`MobileNoteActionsSheet`); дубль-блок удалён. **Безопасность:** `npm audit` → 0 уязвимостей (root + `functions/`, было 23+9). **Строгий TS:** включён `noUncheckedIndexedAccess` (исправлено ~182 места, без `any`/`@ts-ignore`); добавлены type-aware ESLint-правила (`no-floating-promises`, `no-misused-promises`, `strict-boolean-expressions`). **Границы слоёв (T5):** листья `i18n`/`reportError`/`logger`/`errorTypes`/`storageKeys` вынесены `core → shared` (хирургически, без каскада; «бизнес»-мапперы ошибок остались в core) — `shared→core` импортов теперь 0. **Дизайн-токены/inline-стили:** статические inline-стили переведены в utility-классы (279→48), динамические (цвета меток, ширина прогресс-баров, heatmap) оставлены inline и проверены. **Касты:** сокращены (~64→~14). **Качество/инфра:** добавлены Playwright e2e, RUM через web-vitals, проверка регрессии размера бандла в CI, и 100+ новых модульных тестов (421→525). Полный гейт зелёный: ESLint 0, `tsc` 0, vitest 525, сборка Cloud Functions, прод-сборка Vite + prerender. *Примечание: массовая миграция остальных raw-кнопок на `<Button>` (≈110, не баги — рабочие) отложена на отдельный проход.*
- **[EN]** Remaining audit tech-debt + an important bug fix. **Fix (prod):** in the mobile note-actions menu every action was shown twice — the v0.7.11 button migration added new `<Button>`s but left the old `<button>` duplicates in place (`MobileNoteActionsSheet`); the duplicate block was removed. **Security:** `npm audit` → 0 vulnerabilities (root + `functions/`, was 23+9). **Strict TS:** enabled `noUncheckedIndexedAccess` (~182 sites fixed, no `any`/`@ts-ignore`); added type-aware ESLint rules (`no-floating-promises`, `no-misused-promises`, `strict-boolean-expressions`). **Layer boundary (T5):** moved the `i18n`/`reportError`/`logger`/`errorTypes`/`storageKeys` leaves `core → shared` (surgical, no cascade; the "business" error mappers stay in core) — `shared→core` imports are now 0. **Design tokens / inline styles:** static inline styles converted to utility classes (279→48); dynamic ones (label colors, progress-bar widths, heatmap) kept inline and verified. **Casts:** reduced (~64→~14). **Quality/infra:** added Playwright e2e, RUM via web-vitals, a CI bundle-size regression check, and 100+ new unit tests (421→525). Full gate green: ESLint 0, `tsc` 0, vitest 525, Cloud Functions build, Vite production build + prerender. *Note: mass migration of the remaining raw buttons to `<Button>` (~110, not bugs — working) is deferred to a separate pass.*

## 2026-06-01 (v0.7.11)
- **[RU]** Продолжение крупного рефакторинга по аудиту (рефакторинг — **поведение не меняется**; гейт зелёный + smoke-проверка сохранения/удаления/навигации залогиненным и гостем). **Дизайн-система:** ~270 «сырых» `<button>` и поля ввода переведены на единые примитивы (`Button`/`Input`/`Textarea`/`Label`/`FormField`/`Card`/`Badge`); остаток raw-кнопок 322→50 (уникальные места). **Типобезопасность (Epic 8):** парсинг данных из Firestore через мапперы `documentFromDb`/`versionFromDb` вместо `as Document`; типизирован `setSessionConfig`. **Строгий TS (Epic 9):** включены `exactOptionalPropertyTypes` + `forceConsistentCasingInFileNames` (~90 ошибок исправлены настоящими фиксами, без `any`/`@ts-ignore`); `noUncheckedIndexedAccess` сознательно отложен (182 механических правки — отдельной волной). **Typed-lint (Epic 10):** включены `no-floating-promises`, `no-misused-promises`, `strict-boolean-expressions` (лояльная конфигурация). **Токены (Epic 11):** добавлены `--font-size-*`/`--line-height-*` для типографики. **CI (Epic 12):** добавлен `npm audit --audit-level=moderate`; убран модульный кросс-стор `subscribe`. **Граница слоёв:** `cn` вынесен из `core` в `shared`. **Исправление, найденное при smoke:** в desktop-версии модального окна завершения сессии кнопка «Сохранить» из-за ошибки миграции была перевязана не на тот обработчик и не сохраняла заметку — восстановлено (мобильная версия не была затронута). Тесты: **421** зелёных. Полный гейт: ESLint (0), `tsc` (0), vitest (421), сборка Cloud Functions, прод-сборка Vite + prerender.
- **[EN]** Continuation of the large audit-driven refactor (refactor — **no behavior change**; gate green + smoke-tested save/delete/navigation for signed-in and guest). **Design system:** ~270 raw `<button>`s and inputs migrated to unified primitives (`Button`/`Input`/`Textarea`/`Label`/`FormField`/`Card`/`Badge`); remaining raw buttons 322→50 (unique cases). **Type safety (Epic 8):** Firestore data parsed via `documentFromDb`/`versionFromDb` mappers instead of `as Document`; typed `setSessionConfig`. **Strict TS (Epic 9):** enabled `exactOptionalPropertyTypes` + `forceConsistentCasingInFileNames` (~90 errors fixed properly, no `any`/`@ts-ignore`); `noUncheckedIndexedAccess` deliberately deferred (182 mechanical fixes — separate wave). **Typed-lint (Epic 10):** enabled `no-floating-promises`, `no-misused-promises`, `strict-boolean-expressions` (lenient config). **Tokens (Epic 11):** added `--font-size-*`/`--line-height-*` typography tokens. **CI (Epic 12):** added `npm audit --audit-level=moderate`; removed the module-level cross-store `subscribe`. **Layer boundary:** moved `cn` from `core` to `shared`. **Fix found during smoke:** in the desktop session-finish modal, the "Save" button had been wired to the wrong handler during migration and didn't persist the note — restored (mobile was unaffected). Tests: **421** green. Full gate: ESLint (0), `tsc` (0), vitest (421), Cloud Functions build, Vite production build + prerender.

## 2026-06-01 (v0.7.10)
- **[RU]** Крупный внутренний рефакторинг по результатам архитектурного аудита (рефакторинг — **поведение не меняется**, проверено полным гейтом и smoke-тестом основного потока). **Распилены God-объекты/компоненты:** `StorageService` (579 → 124 строк) разделён на `LocalStorageService` / `CloudSyncService` / `ConflictResolver` / `ProfileUpdater` (+ общий `storageTypes`), с сохранением тонкого фасада `StorageService` для обратной совместимости; `AuthContext` разбит на `AuthContext` / `ProfileContext` / `AnalyticsContext`; `WritingPage` разнесён на orchestrator + `WritingSessionContext` (убран `desktopProps` useMemo на 27 зависимостей и prop-drilling в `DesktopWritingLayout`). **Дедуп:** общая логика черновиков cloud/guest вынесена в `useDraftSession`. **Безопасность/чистота:** крипто-разблокировка сейфа вынесена из `LoginPage` в `AuthService` (+ убраны `as string`). **Дизайн-система:** добавлены примитивы `Input`/`Textarea`/`Label`/`FormField`/`Card`/`Badge` (с a11y и тестами) и barrel-экспорт; `Button` расширен. **Доступность:** всем кнопкам в диалогах добавлен `type="button"`; `StreakDots` получил `role`/`aria-label`; хардкод цветов `red-500/400` заменён на токен `accent-danger`. **Сборка/линт:** `manualChunks` пополнен `vendor-ai`/`vendor-sentry`/`vendor-analytics`/`vendor-virtuoso`, `sourcemap` отключён в проде; ESLint усилен (`import/no-cycle: error`, `react-hooks/exhaustive-deps: error`, `no-console` без `info`); `cn` вынесен из `core` в `shared` (исправлена граница слоёв). **Исправление прав:** облачный автосейв черновика (`drafts/{uid}`) для вошедших пользователей больше не падает с `permission-denied` — `WritingDraftService.saveToFirestore` отправляет только поля, разрешённые правилом `isValidDraft` (раньше уходило транзиентное поле `status`, которого нет в `hasOnly()`; баг был пред-существующим, проявлялся только у залогиненных). **Мелочи:** защита от гонки инициализации IndexedDB (счётчик поколения), убран мёртвый параметр `useSessionTags`, `handleStart` не пересоздаётся на каждый символ, добавлены `@types/file-saver`/`@types/jszip`. Тесты: **399 → 421** (добавлены характеризующие тесты ядра и тесты UI-примитивов). Весь гейт зелёный: ESLint (0), `tsc`, vitest (421), сборка Cloud Functions, прод-сборка Vite + prerender.
- **[EN]** Large internal refactor following the architecture audit (refactor — **no behavior change**, verified by the full gate and a smoke test of the main flow). **God objects/components split:** `StorageService` (579 → 124 lines) decomposed into `LocalStorageService` / `CloudSyncService` / `ConflictResolver` / `ProfileUpdater` (+ shared `storageTypes`), keeping a thin `StorageService` facade for backward compatibility; `AuthContext` split into `AuthContext` / `ProfileContext` / `AnalyticsContext`; `WritingPage` split into an orchestrator + `WritingSessionContext` (removed the 27-dependency `desktopProps` useMemo and prop-drilling in `DesktopWritingLayout`). **Dedup:** shared cloud/guest draft logic extracted into `useDraftSession`. **Security/cleanliness:** vault-unlock crypto moved out of `LoginPage` into `AuthService` (+ removed `as string` casts). **Design system:** added `Input`/`Textarea`/`Label`/`FormField`/`Card`/`Badge` primitives (with a11y and tests) and a barrel export; extended `Button`. **Accessibility:** all dialog buttons got `type="button"`; `StreakDots` got `role`/`aria-label`; hardcoded `red-500/400` colors replaced with the `accent-danger` token. **Build/lint:** `manualChunks` extended with `vendor-ai`/`vendor-sentry`/`vendor-analytics`/`vendor-virtuoso`, `sourcemap` disabled in production; ESLint hardened (`import/no-cycle: error`, `react-hooks/exhaustive-deps: error`, `no-console` without `info`); `cn` moved from `core` to `shared` (layer boundary fixed). **Permissions fix:** cloud draft autosave (`drafts/{uid}`) for signed-in users no longer fails with `permission-denied` — `WritingDraftService.saveToFirestore` now sends only the fields allowed by the `isValidDraft` rule (a transient `status` field was being sent, which `hasOnly()` rejects; a pre-existing bug that only affected signed-in users). **Small fixes:** guard against an IndexedDB init race (generation counter), removed a dead `useSessionTags` parameter, `handleStart` no longer recreated on every keystroke, added `@types/file-saver`/`@types/jszip`. Tests: **399 → 421** (added core characterization tests and UI-primitive tests). Full gate green: ESLint (0), `tsc`, vitest (421), Cloud Functions build, Vite production build + prerender.

## 2026-06-01 (v0.7.9)
- **[RU]** Горячие фиксы продакшена + ремедиация внешнего аудита кода. **ИИ-чат восстановлен:** потоковый эндпойнт `/api/chat` падал с `500` на каждый запрос — после дедупликации промптов в 0.7.8 относительный ESM-импорт `../src/shared/ai/prompts` остался без расширения `.js`, и собранная функция падала на загрузке модуля (`ERR_MODULE_NOT_FOUND`, проект собран как `"type":"module"`); из-за этого чат каждый раз уходил в запасной режим (callable) и упирался в дневной лимит. Причина найдена по живым логам Vercel, импорт исправлен на `prompts.js`, стриминг работает. **Права доступа:** ошибки «Missing or insufficient permissions» при сохранении анализа заметки (`AISummaryService`), психологического портрета (`AIProfileService`) и части операций архива — в продакшене были выкачены **устаревшие** правила Firestore (без блока `users/{uid}/summaries` и без ключа `aiPortrait` в обновлении профиля), Firestore не каскадит правила вниз → отказ; актуальные `firestore.rules` задеплоены в обе базы (проверено REST-тестом: запись снова проходит). Также восстановлена пустая переменная `FIREBASE_SERVICE_ACCOUNT` в окружении Vercel. **Аудит — Безопасность (P0):** общий суточный лимит ИИ `withinGlobalDailyLimit()` теперь проверяется в `editWithAI`, `summarizeDocument` и `validateCustomPrompt` (раньше только в `chatWithAI`) — закрыт обход общей квоты; `setUserRole` переписан — `HttpsError` вынесен из транзакции (внутри бросается plain `Error`, конвертируется снаружи), а при сбое `setCustomUserClaims` роль в Firestore откатывается (устранён рассинхрон Firestore ↔ Auth); `sanitizeAiInput` усилен (role-делимитеры `<|im_start|>`/`[INST]`/`<developer>`/`<end_of_turn>` и вырезание zero-width/Unicode-управляющих символов); `userIdSchema` запрещает символы path-traversal. **Аудит — Целостность данных (P1):** при `QuotaExceededError` локальная запись версии больше не «теряется» с пометкой об успешной сессии (профиль не обновляется при провале записи); `useDocuments` — устранён вечный спиннер при пустом `userId` и добавлена отмена in-flight запроса (нет гонок/`setState` на размонтированном); `SessionEditor` теперь сохраняет изменённые теги; чтение документов/версий устойчиво к рассинхрону схемы (записи не пропадают молча), а массовые операции с тегами/лейблами в `DocumentService` больше не проглатывают ошибки. **Аудит — Надёжность/производительность (P2/P3):** добавлен `firestore.indexes.json` (collection-group индекс `daily.date`) и подключён в `firebase.json` для обеих БД; `UnifiedSessionLoader` грузит облачные документы с ограничением параллельности; `WritingDraftService.loadDraft` получил таймаут; два механизма автосохранения черновика консолидированы в один (`useDraftCore`/`wrapSave`, с корректным статусом ошибки и leading-edge сохранением); `AIService` — `analytics.track` убран с критического пути, добавлены таймауты; устранён stale-closure в оптимистичном откате `useAIChat`. **Аудит — Поддерживаемость (P4):** удалён мёртвый код (`functions/src/shared/errors.ts`), убраны дубли констант, добавлены i18n-переводы (`translations/ai.ts`) вместо хардкода, переименован вводящий в заблуждение «геттер» (`isEncryptionEnabled` → `getEncryptionEnabled`), обновлены тесты. **Верификация аудита:** из ~82 ключевых пунктов внешнего аудита ~48 приняты и исправлены, ~17 отклонены как ложные/завышенные (в т.ч. мнимая ошибка сигнатуры `maybeEncrypt` — «фикс» сломал бы шифрование; мнимые дыры удаления чужих документов — граница безопасности это правила Firestore, а не клиент) — регресса нет. Вся проверочная батарея зелёная: ESLint (0 предупреждений при `--max-warnings 0`), `tsc --noEmit`, сборка Cloud Functions и 399 фронтенд-тестов.
- **[EN]** Production hotfixes + external code-audit remediation. **AI chat restored:** the streaming `/api/chat` endpoint returned `500` on every request — after the 0.7.8 prompt deduplication the relative ESM import `../src/shared/ai/prompts` was left without a `.js` extension, so the built function crashed on module load (`ERR_MODULE_NOT_FOUND`, the project is `"type":"module"`); as a result the chat fell back to the callable every time and hit the daily limit. Root-caused from live Vercel logs, fixed the import to `prompts.js`; streaming works. **Permissions:** "Missing or insufficient permissions" errors when saving a note analysis (`AISummaryService`), the psychological portrait (`AIProfileService`), and some archive operations — production had **stale** Firestore rules deployed (missing the `users/{uid}/summaries` block and the `aiPortrait` key on profile updates); Firestore does not cascade rules downward → denial; the current `firestore.rules` were deployed to both databases (verified via a REST test: writes pass again). The empty `FIREBASE_SERVICE_ACCOUNT` env var in Vercel was also restored. **Audit — Security (P0):** the global daily AI cap `withinGlobalDailyLimit()` is now enforced in `editWithAI`, `summarizeDocument`, and `validateCustomPrompt` (previously only in `chatWithAI`), closing a project-wide quota bypass; `setUserRole` was rewritten — `HttpsError` moved out of the transaction (a plain `Error` is thrown inside and converted outside) and the Firestore role is rolled back if `setCustomUserClaims` fails (removing a Firestore ↔ Auth desync); `sanitizeAiInput` was hardened (role delimiters `<|im_start|>`/`[INST]`/`<developer>`/`<end_of_turn>` and stripping of zero-width/Unicode control chars); `userIdSchema` rejects path-traversal characters. **Audit — Data integrity (P1):** on `QuotaExceededError` a local version write is no longer "lost" while the session is marked saved (the profile is not updated when the write fails); `useDocuments` — fixed an infinite spinner when `userId` is empty and added in-flight cancellation (no races / `setState` on unmounted); `SessionEditor` now persists edited tags; document/version reads tolerate schema drift (records no longer vanish silently), and bulk tag/label operations in `DocumentService` no longer swallow errors. **Audit — Reliability/performance (P2/P3):** added `firestore.indexes.json` (collection-group index `daily.date`) wired into `firebase.json` for both databases; `UnifiedSessionLoader` loads cloud documents with bounded concurrency; `WritingDraftService.loadDraft` got a timeout; the two draft-autosave mechanisms were consolidated into one (`useDraftCore`/`wrapSave`, with proper error status and leading-edge save); `AIService` moved `analytics.track` off the critical path and added timeouts; a stale closure in the `useAIChat` optimistic rollback was fixed. **Audit — Maintainability (P4):** removed dead code (`functions/src/shared/errors.ts`), de-duplicated constants, added i18n translations (`translations/ai.ts`) instead of hardcoded strings, renamed a misleading "getter" (`isEncryptionEnabled` → `getEncryptionEnabled`), and updated tests. **Audit verification:** of the ~82 key findings, ~48 were accepted and fixed and ~17 rejected as false/overstated (including a bogus `maybeEncrypt` signature "bug" whose "fix" would have broken encryption, and bogus cross-user delete vulnerabilities — the security boundary is Firestore rules, not the client) — no regressions. The full check suite is green: ESLint (0 warnings at `--max-warnings 0`), `tsc --noEmit`, the Cloud Functions build, and 399 frontend tests.

## 2026-05-31 (v0.7.8)
- **[RU]** Технический аудит проекта (tech-debt / архитектура / system-design / код-ревью / стратегия тестирования / документация) и устранение всех найденных проблем. **CI снова зелёный:** исправлены 3 ошибки lint `@typescript-eslint/no-explicit-any` (`api/chat.ts` — типизирован ключ сервис-аккаунта как `ServiceAccount`; `DiagnosticsPage.tsx` — `catch (e: unknown)` с проверкой `instanceof Error`), а в пайплайн CI добавлен прогон сборки и тестов Cloud Functions (`cd functions && npm ci && npm run build && npm test`), которые раньше не запускались автоматически. **Безопасность:** `/api/chat` приведён к одному уровню защиты с вызываемой функцией — добавлена санитизация входа (нейтрализация маркеров-ролей `<|system|>`/`<|user|>`/`<|assistant|>`) для сообщений и текста документа; в `firestore.rules` ограничен размер поля `aiPortrait` (≤ 100 000 символов) на create и update; документация (`ARCHITECTURE.md`, `SECURITY.md`) приведена в соответствие с реальностью — явно указано, что App Check **не** форсируется (`enforceAppCheck: false`), защита держится на Firebase Auth + лимитах. **Производительность/стоимость:** глобальный суточный лимит ИИ переведён со сканирования `collectionGroup('daily')` на каждом запросе (чтение O(DAU)) на единый агрегатный счётчик `aiGlobalDaily/{date}`, который атомарно инкрементируется батчем вместе с `aiUsage/{uid}/daily/{date}` (и в `chatWithAI`, и в `/api/chat`); счётчик закрыт правилом `read,write: if false`. **Архитектура:** устранено тройное дублирование промптов персон — `/api/chat` теперь импортирует канонический `src/shared/ai/prompts.ts` (удалено ~140 строк инлайна), а копия в `functions/src/shared/prompts.ts` защищена parity-тестом от рассинхрона; схема валидации `/api/chat` синхронизирована с `chatWithAI` (`documentContent` до 50 000, общий объём сообщений ≤ 200 000, `personaId` как enum). **Тесты:** в `functions/` добавлен vitest и тесты для AI-логики (паттерны защиты от инъекций, `sanitizeAiInput`, parity схем) — 15 новых тестов. **Поддерживаемость:** распилены раздутые компоненты — `AIPage` (715 → 394), `DiagnosticsPage` (1071 → 485), `SyncDiagnostics` (808 → 359) с выносом логики в хуки (`useAIPageData`, `useDiagnosticsData`, `useSyncDiagnostics`) и презентационные компоненты; в `chatWithAI` ошибка Gemini больше не проглатывается (логируется). **Документация:** добавлен справочник API ИИ-эндпоинтов (`docs/api/ai-endpoints.md`) и набор тикетов аудита (`docs/TICKETS.md`); из git удалён закоммиченный артефакт сборки `bundle-stats.html` (~1.5 МБ). Все проверки проходят: typecheck, lint, 399 фронтенд-тестов и 15 тестов функций.
- **[EN]** Project technical audit (tech-debt / architecture / system-design / code review / testing strategy / documentation) and remediation of every finding. **CI is green again:** fixed 3 `@typescript-eslint/no-explicit-any` lint errors (`api/chat.ts` — service-account key typed as `ServiceAccount`; `DiagnosticsPage.tsx` — `catch (e: unknown)` with an `instanceof Error` guard), and wired Cloud Functions build + tests into the CI pipeline (`cd functions && npm ci && npm run build && npm test`), which previously never ran automatically. **Security:** `/api/chat` now matches the callable's hardening — added input sanitization (neutralizing the `<|system|>`/`<|user|>`/`<|assistant|>` role markers) for messages and document text; `firestore.rules` caps the `aiPortrait` field size (≤ 100,000 chars) on create and update; docs (`ARCHITECTURE.md`, `SECURITY.md`) were reconciled with reality — it is now stated explicitly that App Check is **not** enforced (`enforceAppCheck: false`) and protection relies on Firebase Auth + rate limits. **Performance/cost:** the global daily AI limit moved from scanning `collectionGroup('daily')` on every request (O(DAU) reads) to a single aggregate counter `aiGlobalDaily/{date}`, incremented atomically in a batch alongside `aiUsage/{uid}/daily/{date}` (in both `chatWithAI` and `/api/chat`); the counter is locked down with `read,write: if false`. **Architecture:** eliminated the triple-duplicated persona prompts — `/api/chat` now imports the canonical `src/shared/ai/prompts.ts` (~140 inline lines removed), and the `functions/src/shared/prompts.ts` copy is guarded by a parity test against drift; the `/api/chat` validation schema was synced with `chatWithAI` (`documentContent` up to 50,000, total messages ≤ 200,000, `personaId` as an enum). **Tests:** added vitest to `functions/` with tests for the AI logic (injection-guard patterns, `sanitizeAiInput`, schema parity) — 15 new tests. **Maintainability:** split oversized components — `AIPage` (715 → 394), `DiagnosticsPage` (1071 → 485), `SyncDiagnostics` (808 → 359), extracting logic into hooks (`useAIPageData`, `useDiagnosticsData`, `useSyncDiagnostics`) and presentational components; `chatWithAI` no longer swallows the Gemini error (it is logged). **Documentation:** added an AI endpoints API reference (`docs/api/ai-endpoints.md`) and the audit ticket set (`docs/TICKETS.md`); removed the committed build artifact `bundle-stats.html` (~1.5 MB) from git. All checks pass: typecheck, lint, 399 frontend tests, and 15 functions tests.

## 2026-05-31 (v0.7.7)
- **[RU]** Доработки ИИ-чата «Собеседник» и инфраструктуры лимитов. Чат теперь раскрывается на весь экран (full-bleed-раскладка: убраны верхний/правый отступы и конфликт высоты, диалог и поле ввода занимают всю высоту и ширину окна, область сообщений расширена), исправлено выравнивание сообщений. Ответы ИИ больше не обрываются на полуслове: поднят лимит вывода модели (`maxOutputTokens` 1024 → 8192, который «съедали» thinking-токены `gemini-2.5-flash`), а Service Worker перестал перехватывать `/api`-запросы и префетч, из-за чего рвался стрим и возникали ошибки «failed to fetch». Исправлена сквозная статистика и лимиты ИИ: потоковый эндпойнт `/api/chat` и облачные функции переведены на одну именованную базу Firestore (`ai-studio-…`) — раньше `/api/chat` и фронтенд писали лимит в `(default)`, а функции читали из именованной БД, из-за чего админ-статистика была пустой, а сброс лимита бил мимо; теперь расход токенов фиксируется (`aiUsage/{uid}/daily/{date}`) для каждого запроса. Добавлен админ-дашборд расхода ИИ (Диагностика → «Статистика AI»): запросы и токены по пользователям за день, суммарные итоги и сравнение с лимитами тарифа **Gemini API Tier 1** для `gemini-2.5-flash` (RPM 1 000 / TPM 1 000 000 / RPD 10 000) — с прогресс-барами и индикатором превышения. Введены глобальные суточные лимиты на бэкенде (общий потолок запросов и токенов по всем пользователям с учётом сбросов), проверяемые перед каждым запросом и в `/api/chat`, и в `chatWithAI`; настраиваются через env (`AI_TIER_RPD/TPD/RPM/TPM`, `AI_DAILY_LIMIT`). Сброс суточного лимита снова работает (в таблице пользователей и вручную по UID) — исправлена IAM-привязка `allUsers`/`run.invoker` на вызываемых функциях (отсутствовала у `resetUserLimit` и `getAILimit`, из-за чего preflight падал с CORS), отключена принудительная проверка App Check, добавлен защитный бюджет от обнуления при аномально высоком общем расходе. Починена генерация психологического портрета пользователя (раньше сохранялся «отказ» старой персоны «группа психологов»; теперь используется кастомная персона-психоаналитик) и добавлена кнопка «Сгенерировать / Обновить» в диагностике. Добавлен надёжный откат на облачную функцию `chatWithAI`, если потоковый ответ не удался.
- **[EN]** AI "Companion" chat polish and limit-infrastructure fixes. The chat now opens full-screen (full-bleed layout: removed the top/right padding and the height conflict so the conversation and input fill the viewport height and width, with a wider message area) and message alignment was fixed. AI replies no longer get cut off mid-sentence: the model output cap was raised (`maxOutputTokens` 1024 → 8192, which `gemini-2.5-flash` thinking tokens were exhausting) and the Service Worker no longer intercepts `/api` calls and prefetch requests, which had been cutting the stream and causing "failed to fetch" errors. End-to-end AI stats and limits were fixed: the streaming `/api/chat` endpoint and the Cloud Functions were unified onto a single named Firestore database (`ai-studio-…`) — previously `/api/chat` and the frontend wrote the limit to `(default)` while the functions read the named DB, which left admin stats empty and made limit resets miss; token usage is now recorded (`aiUsage/{uid}/daily/{date}`) for every request. An admin AI-usage dashboard was added (Diagnostics → "AI Stats"): per-user daily requests/tokens, totals, and a comparison against **Gemini API Tier 1** limits for `gemini-2.5-flash` (RPM 1,000 / TPM 1,000,000 / RPD 10,000) with progress bars and an over-limit indicator. Global daily backend caps were introduced (a project-wide ceiling on requests and tokens across all users, including resets), checked before every request in both `/api/chat` and `chatWithAI`; configurable via env (`AI_TIER_RPD/TPD/RPM/TPM`, `AI_DAILY_LIMIT`). Resetting the daily limit works again (from the users table and manually by UID) — the `allUsers`/`run.invoker` IAM binding was fixed on the callable functions (missing on `resetUserLimit` and `getAILimit`, which made the preflight fail with CORS), enforced App Check was disabled, and a safety budget was added to block resets when overall usage is abnormally high. User psychological-portrait generation was fixed (it used to save the old "group of psychologists" persona's refusal; it now uses a custom psychoanalyst persona) and a "Generate / Refresh" button was added in diagnostics. A robust fallback to the `chatWithAI` Cloud Function was added when the streaming response fails.

## 2026-05-30 (v0.7.6)
- **[RU]** Полный редизайн экрана «Собеседник» (ИИ-чат) в фирменном визуальном языке: монограммы-аватары в цвете каждой персоны, шапка с именем и ролью активной персоны, ответы ИИ в формате письма с цветной вертикальной линейкой и действием «копировать», градиентные сообщения пользователя, обновлённый список диалогов (монограммы, акцент активного, превью) и поле ввода на всю ширину с индикатором лимита. По кнопке ⓘ на персоне открывается её описание и системный промпт; пользовательские персоны можно редактировать (промпт/имя) и удалять, встроенные — только для просмотра (их промпты канонические и применяются на сервере). Дублирующая кнопка «Прикрепить заметку» убрана (функция осталась в меню «+»). На экране письма шестерёнка «Настройки» теперь открывает закрепляемую панель (вкладка в Life Log): при закреплении редактор сдвигается, а панель не перекрывает текст. Исправления бэкенда: в ИИ-эндпойнте `/api/chat` устранена ошибка ESM-импорта `firebase-admin` и обновлены модель/ключ Gemini; добавлен надёжный разбор ключа сервисного аккаунта Firebase (обёрнутые кавычки, отсутствие внешних фигурных скобок); Firestore переведён на long-polling в продакшене, что убирает ошибки `ERR_QUIC_PROTOCOL_ERROR` в консоли.
- **[EN]** Full redesign of the AI "Companion" (chat) screen in the brand visual language: persona-coloured monogram avatars, a header showing the active persona’s name and role, letter-style AI replies framed by a coloured rule with a "copy" action, gradient user bubbles, a refreshed dialogue list (monograms, active accent, previews) and a full-width input with a usage meter. An ⓘ button on each persona opens its description and system prompt; custom personas can be edited (prompt/name) and deleted, while built-in personas are read-only (their prompts are canonical and applied server-side). The duplicate "Attach note" button was removed (still available in the "+" menu). On the writing screen, the Settings gear now opens the pinnable panel (Life Log tab): when pinned, the editor shifts aside instead of being covered. Backend fixes: the `/api/chat` AI endpoint no longer throws the `firebase-admin` ESM import error and uses the updated Gemini model/key; robust parsing of the Firebase service-account key was added (wrapped quotes, missing outer curly braces); and Firestore is forced to long-polling in production, removing `ERR_QUIC_PROTOCOL_ERROR` console noise.

## 2026-05-29 (v0.7.5)
- **[RU]** Полное удаление legacy-функционала сессий и очистка кодовой базы от следов коллекции `sessions`. Удалены неиспользуемые сервисы и хуки: `SessionService`, `WritingSessionService`, `AdminSessionService`, `LocalSessionLoader`, `useSessionList`, скрипт `firestore-migrate-versions.ts` и связанные с ними тесты. Рефакторинг `SyncService` (удалена миграция старых сессий) и `SyncDiagnostics` (удалена вкладка legacy сессий, а также связанные с ней статусы и обработчики). В правилах базы данных `firestore.rules` и схеме `firebase-blueprint.json` полностью вырезан путь `/sessions/{sessionId}`. В Cloud Function `editWithAI` удален неиспользуемый параметр `sessionId`. Все тесты и сборка приложения успешно перепроверены и проходят корректно.
- **[EN]** Complete removal of legacy sessions functionality and database cleanup of the `sessions` collection. Removed obsolete services, hooks, and tests: `SessionService`, `WritingSessionService`, `AdminSessionService`, `LocalSessionLoader`, `useSessionList`, the `firestore-migrate-versions.ts` script, and their tests. Refactored `SyncService` (removed legacy session migrations) and `SyncDiagnostics` (removed legacy session listings, statuses, and migration handlers). Deleted paths and schemas for `/sessions/{sessionId}` from `firestore.rules` and `firebase-blueprint.json`. Removed the unused `sessionId` parameter from the `editWithAI` Cloud Function. All client/backend tests and builds verified and passing successfully.

## 2026-05-29 (v0.7.4)
- **[RU]** Ленивая клиентская миграция легаси-сессий с сохранением исторических меток времени (`firstSessionAt`, `lastSessionAt`, `savedAt`, `sessionStartedAt`) и количества сессий в IndexedDB/Firestore. Разрешение конфликтов оффлайн/онлайн синхронизации через форкинг: при обнаружении независимых изменений в облаке локальная версия клонируется в документ-копию с суффиксом конфликтности. Добавлена очередь промисов (Mutex Lock) в `StorageService` для сериализации параллельных сохранений. Инфраструктурные сервисы (`VersionService`, `LocalVersionService`, `DiffService`) перенесены из features в `core/services` для устранения циклических зависимостей. Создана общая папка `src/shared/ai/` с Zod-схемами и промптами, используемая совместно в Firebase Cloud Functions и Vercel Edge routes. Обновлён Zod функций до v4. Исправлена обработка ошибок чтения localStorage в `useEncryptionStore` (возврат `false` при сбое). Устранено дублирование логирования ошибок Sentry в `DocumentService.ts`. Декомпозирован интерфейс `WritingPageUI` на `EditorPanel.tsx` и `TimerDisplay.tsx` с фиксом stale closures в таймере. Добавлены Feature Flags на базе Firebase Remote Config и регламенты эксплуатации (ops runbooks). Настроено разделение кода Vite (manualChunks) и динамическое хэширование кэша Service Worker.
- **[EN]** Client-side lazy migration of legacy sessions with strict preservation of original timestamps (`firstSessionAt`, `lastSessionAt`, `savedAt`, `sessionStartedAt`) and session counts in IndexedDB/Firestore. Offline sync conflict resolution via cloning/forking: local offline modifications are saved as a separate conflict-marked copy when cloud changes conflict. Added a Promise Queue (Mutex Lock) to `StorageService` to serialize concurrent saves. Versioning and diff services (`VersionService`, `LocalVersionService`, `DiffService`) moved to `core/services` to resolve circular imports. Standardized AI schemas and prompts under `src/shared/ai/` shared between Cloud Functions and Vercel Edge API routes; upgraded backend Zod to v4. Improved exception handling in `useEncryptionStore` with a safe `false` fallback on localStorage access errors. Removed double-logging to Sentry in `DocumentService.ts`. Decomposed `WritingPageUI` into `EditorPanel.tsx` and `TimerDisplay.tsx`, stabilizing play/pause callbacks to avoid stale closures. Added Remote Config Feature Flags integration and operations runbooks. Integrated manualChunks bundle splitting in Vite and dynamic Service Worker cache versions.

## 2026-05-29 (v0.7.3)
- **[RU]** Новая архитектура шифрования: отдельный пароль-сейф, независимый от пароля аккаунта (envelope-шифрование — мастер-ключ из пароля оборачивает ключ данных). Новые сервисы `EncryptionService`/`EncryptionMetaService`/`KeySessionService`/`LegacyKeyMigration` и модуль `features/encryption` (модал `EncryptionPasswordModal` с режимами setup/unlock/migrate/change-password). Миграция со старого формата ключей (`encryptionSalt` + `encryptedDataKey`) на новый `encryptionMeta` без потери данных. Минимальная длина пароля-сейфа поднята с 6 до 8 символов. Массовый экспорт всех заметок в ZIP из отдельных `.md`-файлов в «Настройки → Аккаунт» (`ExportAllService`, `AccountExportSection`). Страница диагностики: бейдж «Encrypted» для уже зашифрованных облачных заметок (через `VersionService.getLatestVersion`) и кнопка шифрования отдельного документа. Фикс: зашифрованные облачные заметки больше не экспортируются и не отображаются в архиве как шифротекст — расшифровываются в `UnifiedSessionLoader` при разблокированном сейфе. Архив автоматически обновляется после разблокировки сейфа. Принятие политики конфиденциальности: добавлены `privacyAcceptedAt`/`privacyVersion` в `firestore.rules` (исправлена ошибка прав). Исправлен вход для аккаунтов со старым форматом шифрования. Включение облачной синхронизации предлагает настроить шифрование. Внутреннее: дедупликация хелперов session-key, удалён мёртвый `reEncryptAllData`.
- **[EN]** New encryption architecture: a separate vault password, independent of the account password (envelope encryption — a master key derived from the password wraps the data key). New `EncryptionService`/`EncryptionMetaService`/`KeySessionService`/`LegacyKeyMigration` services and a `features/encryption` module (`EncryptionPasswordModal` with setup/unlock/migrate/change-password modes). Migration from the legacy key format (`encryptionSalt` + `encryptedDataKey`) to the new `encryptionMeta` with no data loss. Vault password minimum length raised from 6 to 8 characters. Bulk export of all notes to a ZIP of separate `.md` files in Settings → Account (`ExportAllService`, `AccountExportSection`). Diagnostics page: "Encrypted" badge for already-encrypted cloud notes (via `VersionService.getLatestVersion`) and a per-document encrypt button. Fix: encrypted cloud-only notes no longer export or appear in the archive as ciphertext — decrypted in `UnifiedSessionLoader` when the vault is unlocked. Archive refreshes automatically after the vault is unlocked. Privacy policy acceptance: added `privacyAcceptedAt`/`privacyVersion` to `firestore.rules` (fixed permission error). Fixed login for accounts on the legacy encryption format. Enabling cloud sync now prompts encryption setup. Internal: deduplicated session-key helpers, removed dead `reEncryptAllData`.

## 2026-05-28 (v0.7.2)
- **[RU]** Реальный стриминг ИИ: интеграция Vercel AI SDK + Gemini 2.5 Flash через Vercel API Routes (`/api/chat`). Ответ появляется по словам в реальном времени вместо имитации. Серверная проверка Firebase ID Token и дневного лимита через Firestore. Убран фейковый typewriter-эффект — заменён настоящим стримингом. Страница диагностики: дерево документов с раскрывающимися версиями, превью контента (300 символов), иконки облака/локального хранилища. Роль администратора: возврат к проверке через Firestore profile вместо ID Token Claims. Маршрут `/diagnostics` исправлен — страница открывается корректно. Счётчик лимита ИИ: убран вызов устаревшей Cloud Function `getAILimit`, теперь читается напрямую из Firestore `aiDailyLimit/{uid}`.
- **[EN]** Real AI streaming: Vercel AI SDK + Gemini 2.5 Flash via Vercel API Routes (`/api/chat`). Response appears word by word in real time instead of simulation. Server-side Firebase ID Token verification and daily limit via Firestore. Removed fake typewriter effect — replaced with real streaming. Diagnostics page: document tree with expandable versions, content preview (300 chars), cloud/local storage icons. Admin role: reverted to Firestore profile check instead of ID Token Claims. Fixed `/diagnostics` route — page opens correctly. AI limit counter: removed outdated `getAILimit` Cloud Function call, now reads directly from Firestore `aiDailyLimit/{uid}`.

## 2026-05-27 (v0.7.1)
- **[RU]** ИИ-чат: эффект печатания для ответов (символы появляются постепенно). Ответы рендерятся в Markdown — жирный, курсив, списки, заголовки. Чат теперь работает без заметки: разговор можно начать свободно, прикрепить заметку опционально. Кнопка «Отправить в ИИ» в финиш-модале сессии. Новая Cloud Function `getAILimit` — счётчик дневного лимита синхронизируется с сервером вместо localStorage. AIPanel в редакторе проверяет лимит до отправки запроса. Описания персон перенесены в i18n (RU + EN). Аналитика PostHog включается только при явном согласии через `localStorage('analytics_consent')`; добавлены `optIn`/`optOut` методы. Проверка роли admin через ID Token Claims (а не Firestore profile) — защита от подмены. Фикс race condition в `editWithAI`: транзакция Firestore вместо последовательных reads. `enforceAppCheck: true` включён на `editWithAI`, история сообщений санируется перед отправкой в Gemini. Смена пароля: при ошибке обновления в Firebase Auth ключи шифрования автоматически откатываются в Firestore. Service Worker: раздельный stale-while-revalidate кеш для навигационных запросов и кеш-first для ассетов. CSP усилен: `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`; добавлены заголовки `COOP: same-origin` и `CORP: same-origin`.
- **[EN]** AI chat: typewriter effect for responses (characters appear gradually). Responses rendered as Markdown — bold, italic, lists, headings. Chat now works without a note: start freely, attach optionally. "Send to AI" button in session finish modal. New `getAILimit` Cloud Function — daily limit counter synced from server instead of localStorage. AIPanel in editor checks limit before sending. Persona descriptions moved to i18n (RU + EN). PostHog analytics only activates on explicit `localStorage('analytics_consent')` consent; `optIn`/`optOut` methods added. Admin role verified via ID Token Claims (not Firestore profile) — prevents spoofing. Fixed race condition in `editWithAI`: Firestore transaction instead of sequential reads. `enforceAppCheck: true` on `editWithAI`, message history sanitised before sending to Gemini. Password change: encryption keys rolled back in Firestore if Firebase Auth update fails. Service Worker: stale-while-revalidate for navigation, cache-first for assets. CSP hardened: `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`; added `COOP: same-origin` and `CORP: same-origin` headers.

## 2026-05-26 (v0.7.0)
- **[RU]** Новый раздел ИИ (`/ai`): чат с 5 встроенными персонами (Группа психологов, КПТ-психолог, Редактор, Коуч, Журналист) и возможностью создавать собственные. Загрузка заметки в диалог — ИИ учитывает текст и настроение. История диалогов, архивирование, скачивание в `.md`. Анализ заметки: тональность, ключевые слова, темы, инсайты — AI-бейдж на карточках в архиве. Портрет пользователя на основе всех саммари, экспорт в `.md`. Лимит 5 запросов/день. Политика конфиденциальности — модал при первом входе. Тогл автосинхронизации в настройках. Скрытая страница диагностики (5 тапов по версии). Раздел AI Usage в админке. Бэкенд: Node.js 20 → 22, firebase-functions 5 → 7.
- **[EN]** New AI section (`/ai`): chat with 5 built-in personas (Psychology Panel, CBT Therapist, Editor, Coach, Journalist) plus custom persona creation. Load a note into the chat — the AI sees your text and mood emoji. Dialogue history, archiving, download as `.md`. Note analysis: tone, key words, themes, insights — AI badge on archive cards. User portrait aggregated from all summaries, export to `.md`. 5 requests/day limit. Privacy policy modal on first login. Auto-sync toggle in settings. Hidden diagnostics page (tap version 5 times). AI Usage tab in admin panel. Backend: Node.js 20 → 22, firebase-functions 5 → 7.

## 2026-05-26 (v0.6.11)
- **[RU]** SEO-аудит: исправлен `lang="en"` → `lang="ru"` в index.html; добавлены canonical, hreflang (RU/EN), Open Graph (type, url, locale), Twitter Card, JSON-LD (WebApplication + Organization). Создан `og-image.svg` 1200×630. Добавлен `sitemap.xml` с 5 публичными страницами. `robots.txt` дополнен директивой `Sitemap:`. Пре-рендер уникальных мета для каждого роута через `scripts/prerender.ts` (встроен в `npm run build`). Новый компонент `SeoHead` (react-helmet-async) — динамические title/description/canonical по языку; подключён на всех публичных страницах (/, /login, /about, /changelog, /features). Исправлен баг hreflang: теги теперь фиксированы (`ru` → базовый URL, `en` → `?lang=en`), canonical переключается по языку пользователя. Новая страница `/features` — SEO-лендинг с описанием возможностей на RU и EN. `manifest.json`: `short_name` "jw" → "justwriting", description переведён на EN. `metadata.json`: description переписан на EN для международных рынков.
- **[RU]** Бренд-аудит: убран эмодзи ⚠️ из предупреждения о сбросе пароля, добавлена иконка `AlertTriangle` в UI-компоненте. `finish_congrats`: "Отличная работа!" → "Сессия завершена." Достижения — убраны «героические» метафоры (бог, воин, король, титан, фанатизм): streak-достижения переименованы в тихие ("Три недели в потоке", "Месяц в письме" и т.д.), word-достижения получили живые, но сдержанные названия ("Первая тысяча", "Набирает силу", "Четверть миллиона"). Достижения за заметки переименованы в "Пять сессий" и т.д. Личный email убран из текста ошибки — заменён ссылкой на страницу «О приложении». В `ErrorBoundary` email обфусцирован для защиты от ботов.
- **[EN]** SEO audit: fixed `lang="en"` → `lang="ru"` in index.html; added canonical, hreflang (RU/EN), Open Graph (type, url, locale), Twitter Card, JSON-LD (WebApplication + Organization). Created `og-image.svg` 1200×630. Added `sitemap.xml` with 5 public pages. `robots.txt` updated with `Sitemap:` directive. Per-route unique meta via `scripts/prerender.ts` (integrated into `npm run build`). New `SeoHead` component (react-helmet-async) — dynamic title/description/canonical by language; added to all public pages (/, /login, /about, /changelog, /features). Fixed hreflang bug: tags are now fixed (`ru` → base URL, `en` → `?lang=en`), canonical switches by user language. New `/features` page — SEO landing with feature descriptions in RU and EN. `manifest.json`: `short_name` "jw" → "justwriting", description translated to EN. `metadata.json`: description rewritten in EN for international markets.
- **[EN]** Brand audit: removed ⚠️ emoji from password reset encryption warning, added `AlertTriangle` icon in UI component. `finish_congrats`: "Great job!" → "Session complete." Achievements — removed heroic metaphors (god, warrior, king, titan, fanatic): streak achievements renamed to quiet forms ("Three-week flow", "Monthly practice", etc.), word achievements got meaningful but calm names ("First thousand", "Gaining ground", "Quarter million"). Notes achievements renamed to "Five sessions" etc. Personal email removed from error text — replaced with link to About page. Email obfuscated in `ErrorBoundary` to protect against bots.

## 2026-05-25 (v0.6.9)
- **[RU]** Исправлено растягивание карточек в сетке архива: убран двойной скролл, добавлен кастомный GridItem-компонент с `max-width: 400px`, сетка теперь ровно 3 колонки (`repeat(3, minmax(0, 1fr))`). Восстановлена группировка по дням в grid-режиме — `Virtuoso` рендерит каждую дату-группу с заголовком + CSS-grid карточек.
- **[EN]** Fixed grid card stretching in archive: removed double-scroll container, added custom GridItem component with `max-width: 400px`, grid now uses exactly 3 columns (`repeat(3, minmax(0, 1fr))`). Restored date grouping in grid mode — `Virtuoso` renders each date group with header + CSS-grid of cards.

## 2026-05-25 (v0.6.8)
- **[RU]** Спринты 1–6 (аудит + архитектура): ProtectedRoute для `/me`, `/archive`, `/profile`; фикс краша `auth.currentUser!.uid`; Zod-валидация + onSnapshot error handler; AuthService (object export); убраны дубли интервалов; `noreferrer` на PDF-экспорт; фикс `hidden="until-found" as any`. Перенос 5 сервисов в `core/services/`; 18+ импортов + 7 тест-моков исправлено; `Session.createdAt` тип `Timestamp | Date | number`; `toTimestampMs()` вместо `as any`; инверсия core→features (FlowPulse `isActive`, SettingsProvider `renderSettingsPanel`); per-route `ErrorBoundary`; консолидация 20+ zustand-селекторов в `useShallow`; `React.memo` на SessionCard; `LoadingSkeleton` компонент; `no-explicit-any: error` в ESLint; `localDb` → `core/storage/`, `firestoreSchemas` → `core/firebase/schemas/`, `firestore-errors` → `core/errors/`; `isFirestoreConnected` fast-fail; `StatCard`/`SettingRow` → shared; тестовые утилиты (mocks, render, factories); Istanbul coverage; `npm run build` в CI.
- **[RU]** TD-01–TD-08: `react-virtuoso` (Virtuoso/VirtuosoGrid/GroupedVirtuoso) заменил `.map()`; `useDraftCore` + `useVisibilitySave`; `EncryptionService` в `core/crypto/`; сплит AccountTab (613→196 строк) в 3 секции; сплит WritingFinishModal (692→401) в 3 компонента; `VersionEncryptPayload` + index signature; `getSearchContext()` в highlightText; 15 P0 тестов (итого 357); `useProfileStats` с Map-кешем 30s TTL.
- **[RU]** UX-фиксы: кастомный скроллбар (`color-mix(in srgb, var(--brand-soft) 45%, transparent)`); сайдбар без backdrop-blur; уменьшены шрифты тегов/лейблов (`text-[10px]`, `text-[9px]`); убран дублирующий лейбл-бейдж в NoteRow; опция «без лейбла» в NoteRow/MobileNoteActionsSheet; римские → i18n `archive_note_count_one/few/many` + `tp()`.
- **[RU]** UX_FIXLIST тикеты: BUG-06 (скроллбар thumb `color-mix`); BUG-07 (grid `auto-fill minmax(280px,1fr)` + custom-scrollbar на VirtuosoGrid); BUG-08 (убрана opacity-25 на сайдбаре); BUG-09 (LifeLogPanel fixed overlay вместо grid-колонки); UX-12 (сайдбар glass `backdrop-blur-xl` + `color-mix`); UX-13 (кнопка настроек перенесена в Sidebar); BUG-10 (mobile breakpoint 768→480px); BUG-06b (`*::-webkit-scrollbar-*`); BUG-07b (`.archive-grid-list` CSS utility class).
- **[RU]** CSS-структура: найден и исправлен stray `}` после scrollbar-правил — `@layer base` закрывался раньше времени, scrollbar/keyframes/utilities были вложены внутрь. Удалены orphaned `.gap-s-xl`/`.space-y-s-xl`/`.space-y-s-lg`.
- **[RU]** AUDIT_TICKETS (21 тикет): A11Y-04 (`--text-subtle` opacity 0.60–0.75 в 5 темах); A11Y-05 (aria-label на Play/Pause/Stop); UX-14 (zen-подсказка `zen_seen_once` localStorage); A11Y-06 (`useFocusTrap` в ConfirmModal/CancelConfirmModal); A11Y-07 (LoadingSpinner `role="status"` + `sr-only`); A11Y-08 (mood emoji `aria-label` + 5 i18n-ключей); COPY-01 (ALL CAPS → нормальный регистр); COPY-02 (Stream Mode toast при Backspace/Delete с debounce 5s); COPY-03 (~50+ RU строк «вы»→«ты»); DS-06 (8 z-index токенов `--z-*`); DS-07 (3 radius токена); DS-08 (3 shadow utility); A11Y-09 (Toggle `ariaLabel` required); A11Y-10 (WPM pulse `useReducedMotion`); A11Y-11 (GoalPopup input `aria-label`); A11Y-12 (EmptyState `type="button"`); A11Y-13 (`aria-live="polite"` в WritingEditor); DS-09 (Toast `accent-success/danger`); COPY-04 («Пропустить» вместо «Не сохранять»); COPY-05 (empty state CTA i18n); DS-10 (LoadingSkeleton `role="progressbar"` + `lines`); бонус: `errorHandler.ts` ключ `auth_error_invalid_credential`.
- **[EN]** Sprints 1–6 (audit + architecture): ProtectedRoute for `/me`, `/archive`, `/profile`; fixed `auth.currentUser!.uid` crash; Zod validation + onSnapshot error handler; AuthService (object export); removed duplicate intervals; `noreferrer` on PDF export; fixed `hidden="until-found" as any`. Moved 5 services to `core/services/`; fixed 18+ imports + 7 test mocks; `Session.createdAt` type `Timestamp | Date | number`; `toTimestampMs()` instead of `as any`; core→features inversion fix (FlowPulse `isActive`, SettingsProvider `renderSettingsPanel`); per-route `ErrorBoundary`; consolidated 20+ zustand selectors into `useShallow`; `React.memo` on SessionCard; `LoadingSkeleton` component; `no-explicit-any: error` in ESLint; `localDb` → `core/storage/`, `firestoreSchemas` → `core/firebase/schemas/`, `firestore-errors` → `core/errors/`; `isFirestoreConnected` fast-fail; `StatCard`/`SettingRow` → shared; test utils (mocks, render, factories); Istanbul coverage; `npm run build` in CI.
- **[EN]** TD-01–TD-08: `react-virtuoso` (Virtuoso/VirtuosoGrid/GroupedVirtuoso) replaced `.map()`; `useDraftCore` + `useVisibilitySave`; `EncryptionService` in `core/crypto/`; split AccountTab (613→196 lines) into 3 sections; split WritingFinishModal (692→401) into 3 components; `VersionEncryptPayload` + index signature; `getSearchContext()` in highlightText; 15 P0 tests (total 357); `useProfileStats` with Map-cache 30s TTL.
- **[EN]** UX fixes: custom scrollbar (`color-mix(in srgb, var(--brand-soft) 45%, transparent)`); sidebar without backdrop-blur; reduced tag/label fonts (`text-[10px]`, `text-[9px]`); removed duplicate label badge in NoteRow; "no label" option in NoteRow/MobileNoteActionsSheet; Roman numerals → i18n `archive_note_count_one/few/many` + `tp()`.
- **[EN]** UX_FIXLIST tickets: BUG-06 (scrollbar thumb `color-mix`); BUG-07 (grid `auto-fill minmax(280px,1fr)` + custom-scrollbar on VirtuosoGrid); BUG-08 (removed opacity-25 on sidebar); BUG-09 (LifeLogPanel fixed overlay instead of grid column); UX-12 (sidebar glass `backdrop-blur-xl` + `color-mix`); UX-13 (settings button moved to Sidebar); BUG-10 (mobile breakpoint 768→480px); BUG-06b (`*::-webkit-scrollbar-*`); BUG-07b (`.archive-grid-list` CSS utility class).
- **[EN]** CSS structure: found and fixed stray `}` after scrollbar rules — `@layer base` was closing prematurely, scrollbar/keyframes/utilities were nested inside. Removed orphaned `.gap-s-xl`/`.space-y-s-xl`/`.space-y-s-lg`.
- **[EN]** AUDIT_TICKETS (21 tickets): A11Y-04 (`--text-subtle` opacity 0.60–0.75 across 5 themes); A11Y-05 (aria-label on Play/Pause/Stop); UX-14 (zen hint `zen_seen_once` localStorage); A11Y-06 (`useFocusTrap` in ConfirmModal/CancelConfirmModal); A11Y-07 (LoadingSpinner `role="status"` + `sr-only`); A11Y-08 (mood emoji `aria-label` + 5 i18n keys); COPY-01 (ALL CAPS → normal case); COPY-02 (Stream Mode toast on Backspace/Delete with 5s debounce); COPY-03 (~50+ RU strings "вы"→"ты"); DS-06 (8 z-index tokens `--z-*`); DS-07 (3 radius tokens); DS-08 (3 shadow utilities); A11Y-09 (Toggle `ariaLabel` required); A11Y-10 (WPM pulse `useReducedMotion`); A11Y-11 (GoalPopup input `aria-label`); A11Y-12 (EmptyState `type="button"`); A11Y-13 (`aria-live="polite"` in WritingEditor); DS-09 (Toast `accent-success/danger`); COPY-04 ("Пропустить" instead of "Не сохранять"); COPY-05 (empty state CTA i18n); DS-10 (LoadingSkeleton `role="progressbar"` + `lines`); bonus: `errorHandler.ts` key `auth_error_invalid_credential`.

## 2026-05-24 (v0.6.7)
- **[RU]** Реализована полная мобильная адаптация (Фазы 1 и 2) и интеграция улучшений для сенсорных экранов: управление локальным/облачным статусом через bottom-sheet по удержанию, тач-переходы в настройках, автопауза в фоновом режиме, нативный экспорт Web Share API, компактный мобильный баннер подключения, мобильное меню действий в архиве и адаптация AI-панели.
- **[EN]** Implemented full mobile adaptation (Phases 1 & 2) and touch-screen enhancements: local/cloud status management via long-press bottom-sheet, tabbed settings touch navigation, auto-pause on backgrounding, native export via Web Share API, compact mobile connection banner, mobile note actions sheet, and mobile AI panel integration.

## 2026-05-24 (v0.6.5)
- **[RU]** Исправлены все 22 тикета надежности, архитектуры и обработки ошибок. Внедрено полное тестовое покрытие для Firestore-сервисов (`DocumentService`, `VersionService`) и хука `useDocuments`. Устранена циклическая зависимость Timer ↔ Content.
- **[EN]** Resolved all 22 reliability, architectural, and error-handling tickets. Implemented comprehensive test coverage for Firestore services (`DocumentService`, `VersionService`) and `useDocuments` hook. Cleared circular dependencies between Timer and Content.

## 2026-05-23 (v0.6.4)
- **[RU]** Исправлены все оставшиеся замечания Code Review Round 2 (безопасность App Check, Mood Check-in сэйвы, рефакторинг дублирования store/helpers).
- **[EN]** Resolved all remaining Code Review Round 2 issues (App Check security, Mood Check-in saves, unified draft helpers).

## 2026-03-25
- **[EN]** Added "Finish by..." smart timer mode with countdown and progress bar.
- **[RU]** Добавлен умный таймер «Закончить к...» с обратным отсчетом и прогресс-баром.
- **[EN]** Implemented "Fully Local" mode and client-side encryption (AES-256) for notes.
- **[RU]** Внедрен режим «Полностью локально» и сквозное шифрование (AES-256) заметок.
